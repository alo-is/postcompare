# PostCompare.eu — Implementation Plan

## Feature
European postal service price comparator website. Users select a shipment type (letter/parcel), weight, origin country, and destination country. The site ranks national postal services by price in EUR with delivery times.

## Goal
Build a fully functional static site with Astro + React that compares postal rates across 32 European countries (EU27 + EFTA + UK). The site supports 3 languages (FR, EN, DE), uses a postal/kraft paper theme, and sources data from YAML files validated by JSON Schema.

## Architecture
- **Static site** generated by Astro at build time
- **React islands** for interactive comparator form and results (client-side filtering)
- **YAML data files** per operator, loaded at build time and passed as props to React components
- **JSON Schema** validation in CI for data integrity
- **i18n** via Astro's built-in routing with `/[lang]/` prefix pattern
- **No backend** — all comparison logic runs client-side in React

## Tech Stack
- Astro 5.x
- React 19
- TypeScript
- Vite (bundled with Astro)
- js-yaml (YAML parsing)
- ajv (JSON Schema validation)
- vitest (testing)
- GitHub Actions (CI)

---

> **For agentic workers:** This plan is designed for subagent-driven-development. Each step is independent where possible — steps within the same group can be parallelized. Steps are sequenced so that each produces a committable unit. Run tests after each test-related step. Commit after each step or logical group.

---

## Step 1 — Initialize Astro project

**Files:** `package.json`, `astro.config.mjs`, `tsconfig.json`

Create the Astro project with React integration and required dependencies.

```bash
npm create astro@latest . -- --template minimal --install --no-git --typescript strict
npx astro add react --yes
npm install js-yaml ajv ajv-formats
npm install -D @types/js-yaml vitest
```

Then replace `astro.config.mjs` with:

```javascript
// astro.config.mjs
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';

export default defineConfig({
  integrations: [react()],
  i18n: {
    defaultLocale: 'fr',
    locales: ['fr', 'en', 'de'],
    routing: {
      prefixDefaultLocale: true,
      redirectToDefaultLocale: true,
    },
  },
  site: 'https://postcompare.eu',
});
```

Update `tsconfig.json`:

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@data/*": ["data/*"]
    }
  }
}
```

Add scripts to `package.json` (merge into existing):

```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "npx tsx scripts/validate-data.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
```

**Commit:** `chore: initialize Astro project with React integration`

---

## Step 2 — Create directory structure

**Files:** Create empty directories

```
data/operators/
data/schema/
public/logos/
src/components/
src/layouts/
src/pages/
src/pages/[lang]/
src/pages/[lang]/operator/
src/i18n/
src/lib/
src/styles/
tests/
scripts/
```

Create placeholder files so directories are tracked:

- `public/logos/.gitkeep`

**Commit:** `chore: create project directory structure`

---

## Step 3 — TypeScript types

**File:** `src/lib/types.ts`

```typescript
// src/lib/types.ts

/** Delivery time range in business days [min, max] */
export type DeliveryDays = [number, number];

/** A single rate tier for letters */
export interface LetterRate {
  name: string;
  max_weight_g: number;
  price_eur: number;
  delivery_days: DeliveryDays;
}

/** A single rate tier for parcels */
export interface ParcelRate {
  name: string;
  max_weight_kg: number;
  price_eur: number;
  delivery_days: DeliveryDays;
  tracking: boolean;
}

/** International zone grouping countries with shared rates */
export interface LetterZone {
  name: string;
  countries: string[];
  rates: LetterRate[];
}

export interface ParcelZone {
  name: string;
  countries: string[];
  rates: ParcelRate[];
}

/** Letter rates for an operator */
export interface LetterRates {
  domestic: LetterRate[];
  international: {
    zones: LetterZone[];
  };
}

/** Parcel rates for an operator */
export interface ParcelRates {
  domestic: ParcelRate[];
  international: {
    zones: ParcelZone[];
  };
}

/** Operator metadata */
export interface OperatorInfo {
  id: string;
  name: string;
  country: string;
  currency: string;
  website: string;
  logo: string;
  last_updated: string;
}

/** Complete operator data as stored in YAML */
export interface OperatorData {
  operator: OperatorInfo;
  letters: LetterRates;
  parcels: ParcelRates;
}

/** Country metadata */
export interface Country {
  code: string;
  name_fr: string;
  name_en: string;
  name_de: string;
  flag: string;
  eurozone: boolean;
}

/** Exchange rate entry */
export interface ExchangeRate {
  currency: string;
  rate_to_eur: number;
  date: string;
}

/** Shipment type */
export type ShipmentType = 'letter' | 'parcel';

/** Search parameters from the form */
export interface SearchParams {
  type: ShipmentType;
  weight: number; // grams for letters, kg for parcels
  origin: string | 'all'; // ISO country code or 'all'
  destination: string | 'domestic'; // ISO country code or 'domestic'
}

/** A single comparison result */
export interface ComparisonResult {
  operator: OperatorInfo;
  productName: string;
  priceEur: number;
  deliveryDays: DeliveryDays;
  tracking: boolean;
  isBestPrice: boolean;
  route: {
    origin: string;
    destination: string;
    isDomestic: boolean;
  };
}

/** Supported languages */
export type Lang = 'fr' | 'en' | 'de';
```

**Commit:** `feat: add TypeScript type definitions`

---

## Step 4 — JSON Schema for operator validation

**File:** `data/schema/operator.schema.json`

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://postcompare.eu/schemas/operator.json",
  "title": "Postal Operator",
  "description": "Rate data for a national postal service operator",
  "type": "object",
  "required": ["operator", "letters", "parcels"],
  "additionalProperties": false,
  "properties": {
    "operator": {
      "type": "object",
      "required": ["id", "name", "country", "currency", "website", "logo", "last_updated"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9-]+$"
        },
        "name": { "type": "string", "minLength": 1 },
        "country": {
          "type": "string",
          "pattern": "^[A-Z]{2}$"
        },
        "currency": {
          "type": "string",
          "pattern": "^[A-Z]{3}$"
        },
        "website": {
          "type": "string",
          "format": "uri"
        },
        "logo": { "type": "string" },
        "last_updated": {
          "type": "string",
          "format": "date"
        }
      }
    },
    "letters": {
      "type": "object",
      "required": ["domestic", "international"],
      "additionalProperties": false,
      "properties": {
        "domestic": {
          "type": "array",
          "items": { "$ref": "#/$defs/letterRate" },
          "minItems": 1
        },
        "international": {
          "type": "object",
          "required": ["zones"],
          "additionalProperties": false,
          "properties": {
            "zones": {
              "type": "array",
              "items": { "$ref": "#/$defs/letterZone" },
              "minItems": 1
            }
          }
        }
      }
    },
    "parcels": {
      "type": "object",
      "required": ["domestic", "international"],
      "additionalProperties": false,
      "properties": {
        "domestic": {
          "type": "array",
          "items": { "$ref": "#/$defs/parcelRate" },
          "minItems": 1
        },
        "international": {
          "type": "object",
          "required": ["zones"],
          "additionalProperties": false,
          "properties": {
            "zones": {
              "type": "array",
              "items": { "$ref": "#/$defs/parcelZone" },
              "minItems": 1
            }
          }
        }
      }
    }
  },
  "$defs": {
    "deliveryDays": {
      "type": "array",
      "items": { "type": "integer", "minimum": 0 },
      "minItems": 2,
      "maxItems": 2
    },
    "letterRate": {
      "type": "object",
      "required": ["name", "max_weight_g", "price_eur", "delivery_days"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "max_weight_g": { "type": "number", "exclusiveMinimum": 0 },
        "price_eur": { "type": "number", "minimum": 0 },
        "delivery_days": { "$ref": "#/$defs/deliveryDays" }
      }
    },
    "parcelRate": {
      "type": "object",
      "required": ["name", "max_weight_kg", "price_eur", "delivery_days", "tracking"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "max_weight_kg": { "type": "number", "exclusiveMinimum": 0 },
        "price_eur": { "type": "number", "minimum": 0 },
        "delivery_days": { "$ref": "#/$defs/deliveryDays" },
        "tracking": { "type": "boolean" }
      }
    },
    "letterZone": {
      "type": "object",
      "required": ["name", "countries", "rates"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "countries": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[A-Z]{2}$" },
          "minItems": 1
        },
        "rates": {
          "type": "array",
          "items": { "$ref": "#/$defs/letterRate" },
          "minItems": 1
        }
      }
    },
    "parcelZone": {
      "type": "object",
      "required": ["name", "countries", "rates"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "countries": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[A-Z]{2}$" },
          "minItems": 1
        },
        "rates": {
          "type": "array",
          "items": { "$ref": "#/$defs/parcelRate" },
          "minItems": 1
        }
      }
    }
  }
}
```

**Commit:** `feat: add JSON Schema for operator YAML validation`

---

## Step 5 — Countries data file

**File:** `data/countries.yaml`

```yaml
# European countries covered by PostCompare.eu
# EU27 + EFTA (CH, NO, IS, LI) + UK = 32 countries

countries:
  - code: "FR"
    name_fr: "France"
    name_en: "France"
    name_de: "Frankreich"
    flag: "🇫🇷"
    eurozone: true
  - code: "DE"
    name_fr: "Allemagne"
    name_en: "Germany"
    name_de: "Deutschland"
    flag: "🇩🇪"
    eurozone: true
  - code: "ES"
    name_fr: "Espagne"
    name_en: "Spain"
    name_de: "Spanien"
    flag: "🇪🇸"
    eurozone: true
  - code: "IT"
    name_fr: "Italie"
    name_en: "Italy"
    name_de: "Italien"
    flag: "🇮🇹"
    eurozone: true
  - code: "GB"
    name_fr: "Royaume-Uni"
    name_en: "United Kingdom"
    name_de: "Vereinigtes Königreich"
    flag: "🇬🇧"
    eurozone: false
  - code: "NL"
    name_fr: "Pays-Bas"
    name_en: "Netherlands"
    name_de: "Niederlande"
    flag: "🇳🇱"
    eurozone: true
  - code: "BE"
    name_fr: "Belgique"
    name_en: "Belgium"
    name_de: "Belgien"
    flag: "🇧🇪"
    eurozone: true
  - code: "PT"
    name_fr: "Portugal"
    name_en: "Portugal"
    name_de: "Portugal"
    flag: "🇵🇹"
    eurozone: true
  - code: "AT"
    name_fr: "Autriche"
    name_en: "Austria"
    name_de: "Österreich"
    flag: "🇦🇹"
    eurozone: true
  - code: "CH"
    name_fr: "Suisse"
    name_en: "Switzerland"
    name_de: "Schweiz"
    flag: "🇨🇭"
    eurozone: false
  - code: "SE"
    name_fr: "Suède"
    name_en: "Sweden"
    name_de: "Schweden"
    flag: "🇸🇪"
    eurozone: false
  - code: "DK"
    name_fr: "Danemark"
    name_en: "Denmark"
    name_de: "Dänemark"
    flag: "🇩🇰"
    eurozone: false
  - code: "NO"
    name_fr: "Norvège"
    name_en: "Norway"
    name_de: "Norwegen"
    flag: "🇳🇴"
    eurozone: false
  - code: "FI"
    name_fr: "Finlande"
    name_en: "Finland"
    name_de: "Finnland"
    flag: "🇫🇮"
    eurozone: true
  - code: "IE"
    name_fr: "Irlande"
    name_en: "Ireland"
    name_de: "Irland"
    flag: "🇮🇪"
    eurozone: true
  - code: "PL"
    name_fr: "Pologne"
    name_en: "Poland"
    name_de: "Polen"
    flag: "🇵🇱"
    eurozone: false
  - code: "CZ"
    name_fr: "Tchéquie"
    name_en: "Czech Republic"
    name_de: "Tschechien"
    flag: "🇨🇿"
    eurozone: false
  - code: "RO"
    name_fr: "Roumanie"
    name_en: "Romania"
    name_de: "Rumänien"
    flag: "🇷🇴"
    eurozone: false
  - code: "HU"
    name_fr: "Hongrie"
    name_en: "Hungary"
    name_de: "Ungarn"
    flag: "🇭🇺"
    eurozone: false
  - code: "GR"
    name_fr: "Grèce"
    name_en: "Greece"
    name_de: "Griechenland"
    flag: "🇬🇷"
    eurozone: true
  - code: "BG"
    name_fr: "Bulgarie"
    name_en: "Bulgaria"
    name_de: "Bulgarien"
    flag: "🇧🇬"
    eurozone: false
  - code: "HR"
    name_fr: "Croatie"
    name_en: "Croatia"
    name_de: "Kroatien"
    flag: "🇭🇷"
    eurozone: true
  - code: "SK"
    name_fr: "Slovaquie"
    name_en: "Slovakia"
    name_de: "Slowakei"
    flag: "🇸🇰"
    eurozone: true
  - code: "SI"
    name_fr: "Slovénie"
    name_en: "Slovenia"
    name_de: "Slowenien"
    flag: "🇸🇮"
    eurozone: true
  - code: "LT"
    name_fr: "Lituanie"
    name_en: "Lithuania"
    name_de: "Litauen"
    flag: "🇱🇹"
    eurozone: true
  - code: "LV"
    name_fr: "Lettonie"
    name_en: "Latvia"
    name_de: "Lettland"
    flag: "🇱🇻"
    eurozone: true
  - code: "EE"
    name_fr: "Estonie"
    name_en: "Estonia"
    name_de: "Estland"
    flag: "🇪🇪"
    eurozone: true
  - code: "CY"
    name_fr: "Chypre"
    name_en: "Cyprus"
    name_de: "Zypern"
    flag: "🇨🇾"
    eurozone: true
  - code: "LU"
    name_fr: "Luxembourg"
    name_en: "Luxembourg"
    name_de: "Luxemburg"
    flag: "🇱🇺"
    eurozone: true
  - code: "MT"
    name_fr: "Malte"
    name_en: "Malta"
    name_de: "Malta"
    flag: "🇲🇹"
    eurozone: true
  - code: "IS"
    name_fr: "Islande"
    name_en: "Iceland"
    name_de: "Island"
    flag: "🇮🇸"
    eurozone: false
  - code: "LI"
    name_fr: "Liechtenstein"
    name_en: "Liechtenstein"
    name_de: "Liechtenstein"
    flag: "🇱🇮"
    eurozone: false
```

**Commit:** `feat: add countries data file with all 32 European countries`

---

## Step 6 — Exchange rates data file

**File:** `data/exchange-rates.yaml`

```yaml
# Exchange rates to EUR for non-eurozone countries
# Used during data collection to convert local prices to EUR
# Rates from ECB reference rates, collected on the date indicated

rates:
  GBP:
    rate_to_eur: 1.16
    date: "2026-04-01"
  CHF:
    rate_to_eur: 0.97
    date: "2026-04-01"
  SEK:
    rate_to_eur: 0.089
    date: "2026-04-01"
  DKK:
    rate_to_eur: 0.134
    date: "2026-04-01"
  NOK:
    rate_to_eur: 0.085
    date: "2026-04-01"
  PLN:
    rate_to_eur: 0.232
    date: "2026-04-01"
  CZK:
    rate_to_eur: 0.040
    date: "2026-04-01"
  RON:
    rate_to_eur: 0.201
    date: "2026-04-01"
  HUF:
    rate_to_eur: 0.0025
    date: "2026-04-01"
  BGN:
    rate_to_eur: 0.511
    date: "2026-04-01"
  ISK:
    rate_to_eur: 0.0066
    date: "2026-04-01"
```

**Commit:** `feat: add exchange rates data for non-eurozone countries`

---

## Step 7 — Sample operator: La Poste (France)

**File:** `data/operators/la-poste-fr.yaml`

```yaml
operator:
  id: "la-poste-fr"
  name: "La Poste"
  country: "FR"
  currency: "EUR"
  website: "https://www.laposte.fr"
  logo: "la-poste-fr.svg"
  last_updated: "2026-04-01"

letters:
  domestic:
    - name: "Lettre verte"
      max_weight_g: 20
      price_eur: 1.29
      delivery_days: [2, 4]
    - name: "Lettre verte"
      max_weight_g: 100
      price_eur: 2.58
      delivery_days: [2, 4]
    - name: "Lettre verte"
      max_weight_g: 250
      price_eur: 4.30
      delivery_days: [2, 4]
  international:
    zones:
      - name: "Europe"
        countries: ["DE", "ES", "IT", "BE", "NL", "PT", "AT", "CH", "GB", "IE", "LU", "SE", "DK", "NO", "FI", "PL", "CZ", "RO", "HU", "GR", "BG", "HR", "SK", "SI", "LT", "LV", "EE", "CY", "MT", "IS", "LI"]
        rates:
          - name: "Lettre internationale"
            max_weight_g: 20
            price_eur: 1.80
            delivery_days: [3, 7]
          - name: "Lettre internationale"
            max_weight_g: 100
            price_eur: 3.60
            delivery_days: [3, 7]
          - name: "Lettre internationale"
            max_weight_g: 250
            price_eur: 5.80
            delivery_days: [3, 7]

parcels:
  domestic:
    - name: "Colissimo"
      max_weight_kg: 1
      price_eur: 6.40
      delivery_days: [2, 3]
      tracking: true
    - name: "Colissimo"
      max_weight_kg: 3
      price_eur: 7.95
      delivery_days: [2, 3]
      tracking: true
    - name: "Colissimo"
      max_weight_kg: 5
      price_eur: 8.95
      delivery_days: [2, 3]
      tracking: true
    - name: "Colissimo"
      max_weight_kg: 10
      price_eur: 13.75
      delivery_days: [2, 3]
      tracking: true
    - name: "Colissimo"
      max_weight_kg: 30
      price_eur: 17.35
      delivery_days: [2, 3]
      tracking: true
  international:
    zones:
      - name: "Europe"
        countries: ["DE", "ES", "IT", "BE", "NL", "PT", "AT", "CH", "GB", "IE", "LU", "SE", "DK", "NO", "FI", "PL", "CZ", "RO", "HU", "GR", "BG", "HR", "SK", "SI", "LT", "LV", "EE", "CY", "MT", "IS", "LI"]
        rates:
          - name: "Colissimo International"
            max_weight_kg: 1
            price_eur: 9.50
            delivery_days: [3, 5]
            tracking: true
          - name: "Colissimo International"
            max_weight_kg: 3
            price_eur: 14.00
            delivery_days: [3, 5]
            tracking: true
          - name: "Colissimo International"
            max_weight_kg: 5
            price_eur: 18.50
            delivery_days: [3, 5]
            tracking: true
          - name: "Colissimo International"
            max_weight_kg: 10
            price_eur: 26.50
            delivery_days: [3, 5]
            tracking: true
```

**Commit:** `feat: add La Poste (France) operator data`

---

## Step 8 — Sample operator: Deutsche Post (Germany)

**File:** `data/operators/deutsche-post-de.yaml`

```yaml
operator:
  id: "deutsche-post-de"
  name: "Deutsche Post"
  country: "DE"
  currency: "EUR"
  website: "https://www.deutschepost.de"
  logo: "deutsche-post-de.svg"
  last_updated: "2026-04-01"

letters:
  domestic:
    - name: "Standardbrief"
      max_weight_g: 20
      price_eur: 0.95
      delivery_days: [1, 2]
    - name: "Kompaktbrief"
      max_weight_g: 50
      price_eur: 1.15
      delivery_days: [1, 2]
    - name: "Großbrief"
      max_weight_g: 500
      price_eur: 1.80
      delivery_days: [1, 2]
  international:
    zones:
      - name: "Europe"
        countries: ["FR", "ES", "IT", "BE", "NL", "PT", "AT", "CH", "GB", "IE", "LU", "SE", "DK", "NO", "FI", "PL", "CZ", "RO", "HU", "GR", "BG", "HR", "SK", "SI", "LT", "LV", "EE", "CY", "MT", "IS", "LI"]
        rates:
          - name: "Standardbrief International"
            max_weight_g: 20
            price_eur: 1.10
            delivery_days: [2, 5]
          - name: "Großbrief International"
            max_weight_g: 500
            price_eur: 3.70
            delivery_days: [2, 5]

parcels:
  domestic:
    - name: "DHL Päckchen S"
      max_weight_kg: 2
      price_eur: 3.99
      delivery_days: [1, 2]
      tracking: false
    - name: "DHL Paket"
      max_weight_kg: 5
      price_eur: 6.99
      delivery_days: [1, 2]
      tracking: true
    - name: "DHL Paket"
      max_weight_kg: 10
      price_eur: 9.49
      delivery_days: [1, 2]
      tracking: true
    - name: "DHL Paket"
      max_weight_kg: 31.5
      price_eur: 16.49
      delivery_days: [1, 2]
      tracking: true
  international:
    zones:
      - name: "EU"
        countries: ["FR", "ES", "IT", "BE", "NL", "PT", "AT", "IE", "LU", "SE", "DK", "FI", "PL", "CZ", "RO", "HU", "GR", "BG", "HR", "SK", "SI", "LT", "LV", "EE", "CY", "MT"]
        rates:
          - name: "DHL Paket International"
            max_weight_kg: 5
            price_eur: 13.99
            delivery_days: [3, 7]
            tracking: true
          - name: "DHL Paket International"
            max_weight_kg: 10
            price_eur: 18.99
            delivery_days: [3, 7]
            tracking: true
          - name: "DHL Paket International"
            max_weight_kg: 31.5
            price_eur: 37.99
            delivery_days: [3, 7]
            tracking: true
      - name: "Europe hors UE"
        countries: ["CH", "GB", "NO", "IS", "LI"]
        rates:
          - name: "DHL Paket International"
            max_weight_kg: 5
            price_eur: 18.99
            delivery_days: [4, 10]
            tracking: true
          - name: "DHL Paket International"
            max_weight_kg: 10
            price_eur: 25.99
            delivery_days: [4, 10]
            tracking: true
```

**Commit:** `feat: add Deutsche Post (Germany) operator data`

---

## Step 9 — Sample operator: Correos (Spain)

**File:** `data/operators/correos-es.yaml`

```yaml
operator:
  id: "correos-es"
  name: "Correos"
  country: "ES"
  currency: "EUR"
  website: "https://www.correos.es"
  logo: "correos-es.svg"
  last_updated: "2026-04-01"

letters:
  domestic:
    - name: "Carta ordinaria"
      max_weight_g: 20
      price_eur: 0.75
      delivery_days: [2, 3]
    - name: "Carta ordinaria"
      max_weight_g: 50
      price_eur: 0.90
      delivery_days: [2, 3]
    - name: "Carta ordinaria"
      max_weight_g: 100
      price_eur: 1.25
      delivery_days: [2, 3]
    - name: "Carta ordinaria"
      max_weight_g: 500
      price_eur: 2.70
      delivery_days: [2, 3]
  international:
    zones:
      - name: "Europe"
        countries: ["FR", "DE", "IT", "BE", "NL", "PT", "AT", "CH", "GB", "IE", "LU", "SE", "DK", "NO", "FI", "PL", "CZ", "RO", "HU", "GR", "BG", "HR", "SK", "SI", "LT", "LV", "EE", "CY", "MT", "IS", "LI"]
        rates:
          - name: "Carta internacional"
            max_weight_g: 20
            price_eur: 1.65
            delivery_days: [3, 7]
          - name: "Carta internacional"
            max_weight_g: 50
            price_eur: 2.35
            delivery_days: [3, 7]
          - name: "Carta internacional"
            max_weight_g: 100
            price_eur: 3.50
            delivery_days: [3, 7]

parcels:
  domestic:
    - name: "Paquete Estándar"
      max_weight_kg: 2
      price_eur: 4.95
      delivery_days: [2, 4]
      tracking: true
    - name: "Paquete Estándar"
      max_weight_kg: 5
      price_eur: 7.50
      delivery_days: [2, 4]
      tracking: true
    - name: "Paquete Estándar"
      max_weight_kg: 10
      price_eur: 10.95
      delivery_days: [2, 4]
      tracking: true
    - name: "Paquete Estándar"
      max_weight_kg: 20
      price_eur: 15.50
      delivery_days: [2, 4]
      tracking: true
  international:
    zones:
      - name: "Europe"
        countries: ["FR", "DE", "IT", "BE", "NL", "PT", "AT", "CH", "GB", "IE", "LU", "SE", "DK", "NO", "FI", "PL", "CZ", "RO", "HU", "GR", "BG", "HR", "SK", "SI", "LT", "LV", "EE", "CY", "MT", "IS", "LI"]
        rates:
          - name: "Paquete Internacional Económico"
            max_weight_kg: 2
            price_eur: 10.50
            delivery_days: [5, 10]
            tracking: true
          - name: "Paquete Internacional Económico"
            max_weight_kg: 5
            price_eur: 16.00
            delivery_days: [5, 10]
            tracking: true
          - name: "Paquete Internacional Económico"
            max_weight_kg: 10
            price_eur: 23.50
            delivery_days: [5, 10]
            tracking: true
```

**Commit:** `feat: add Correos (Spain) operator data`

---

## Step 10 — Sample operator: Royal Mail (UK)

**File:** `data/operators/royal-mail-gb.yaml`

```yaml
operator:
  id: "royal-mail-gb"
  name: "Royal Mail"
  country: "GB"
  currency: "GBP"
  website: "https://www.royalmail.com"
  logo: "royal-mail-gb.svg"
  last_updated: "2026-04-01"

letters:
  domestic:
    - name: "2nd Class"
      max_weight_g: 100
      price_eur: 0.87
      delivery_days: [2, 3]
    - name: "1st Class"
      max_weight_g: 100
      price_eur: 1.33
      delivery_days: [1, 2]
    - name: "2nd Class Large"
      max_weight_g: 250
      price_eur: 1.51
      delivery_days: [2, 3]
    - name: "1st Class Large"
      max_weight_g: 250
      price_eur: 2.15
      delivery_days: [1, 2]
  international:
    zones:
      - name: "Europe"
        countries: ["FR", "DE", "ES", "IT", "BE", "NL", "PT", "AT", "CH", "IE", "LU", "SE", "DK", "NO", "FI", "PL", "CZ", "RO", "HU", "GR", "BG", "HR", "SK", "SI", "LT", "LV", "EE", "CY", "MT", "IS", "LI"]
        rates:
          - name: "International Standard"
            max_weight_g: 20
            price_eur: 2.67
            delivery_days: [5, 7]
          - name: "International Standard"
            max_weight_g: 100
            price_eur: 3.48
            delivery_days: [5, 7]
          - name: "International Standard"
            max_weight_g: 250
            price_eur: 4.64
            delivery_days: [5, 7]

parcels:
  domestic:
    - name: "Royal Mail 48"
      max_weight_kg: 1
      price_eur: 3.95
      delivery_days: [2, 3]
      tracking: true
    - name: "Royal Mail 48"
      max_weight_kg: 2
      price_eur: 4.45
      delivery_days: [2, 3]
      tracking: true
    - name: "Royal Mail 48"
      max_weight_kg: 5
      price_eur: 5.95
      delivery_days: [2, 3]
      tracking: true
    - name: "Royal Mail 48"
      max_weight_kg: 10
      price_eur: 7.25
      delivery_days: [2, 3]
      tracking: true
    - name: "Royal Mail 48"
      max_weight_kg: 20
      price_eur: 11.55
      delivery_days: [2, 3]
      tracking: true
  international:
    zones:
      - name: "Europe"
        countries: ["FR", "DE", "ES", "IT", "BE", "NL", "PT", "AT", "CH", "IE", "LU", "SE", "DK", "NO", "FI", "PL", "CZ", "RO", "HU", "GR", "BG", "HR", "SK", "SI", "LT", "LV", "EE", "CY", "MT", "IS", "LI"]
        rates:
          - name: "International Tracked"
            max_weight_kg: 2
            price_eur: 12.50
            delivery_days: [5, 10]
            tracking: true
          - name: "International Tracked"
            max_weight_kg: 5
            price_eur: 19.80
            delivery_days: [5, 10]
            tracking: true
          - name: "International Tracked"
            max_weight_kg: 10
            price_eur: 30.50
            delivery_days: [5, 10]
            tracking: true
```

**Commit:** `feat: add Royal Mail (UK) operator data`

---

## Step 11 — Data loader module

**File:** `src/lib/data-loader.ts`

```typescript
// src/lib/data-loader.ts
import yaml from 'js-yaml';
import fs from 'node:fs';
import path from 'node:path';
import type { OperatorData, Country } from './types';

const DATA_DIR = path.resolve(process.cwd(), 'data');

/**
 * Load all operator YAML files from data/operators/
 */
export function loadAllOperators(): OperatorData[] {
  const operatorsDir = path.join(DATA_DIR, 'operators');
  const files = fs.readdirSync(operatorsDir).filter((f) => f.endsWith('.yaml'));

  return files.map((file) => {
    const content = fs.readFileSync(path.join(operatorsDir, file), 'utf-8');
    return yaml.load(content) as OperatorData;
  });
}

/**
 * Load a single operator by ID
 */
export function loadOperator(id: string): OperatorData | null {
  const filePath = path.join(DATA_DIR, 'operators', `${id}.yaml`);
  if (!fs.existsSync(filePath)) return null;

  const content = fs.readFileSync(filePath, 'utf-8');
  return yaml.load(content) as OperatorData;
}

/**
 * Load the countries list
 */
export function loadCountries(): Country[] {
  const content = fs.readFileSync(
    path.join(DATA_DIR, 'countries.yaml'),
    'utf-8',
  );
  const data = yaml.load(content) as { countries: Country[] };
  return data.countries;
}

/**
 * Get all operator IDs from the operators directory
 */
export function getOperatorIds(): string[] {
  const operatorsDir = path.join(DATA_DIR, 'operators');
  return fs
    .readdirSync(operatorsDir)
    .filter((f) => f.endsWith('.yaml'))
    .map((f) => f.replace('.yaml', ''));
}
```

**Commit:** `feat: add data loader for YAML operator and country files`

---

## Step 12 — Comparison engine (TDD: tests first)

**File:** `tests/comparison-engine.test.ts`

```typescript
// tests/comparison-engine.test.ts
import { describe, it, expect } from 'vitest';
import { compare } from '../src/lib/comparison-engine';
import type { OperatorData, SearchParams, ComparisonResult } from '../src/lib/types';

// Minimal test fixtures
const laPoste: OperatorData = {
  operator: {
    id: 'la-poste-fr',
    name: 'La Poste',
    country: 'FR',
    currency: 'EUR',
    website: 'https://www.laposte.fr',
    logo: 'la-poste-fr.svg',
    last_updated: '2026-04-01',
  },
  letters: {
    domestic: [
      { name: 'Lettre verte', max_weight_g: 20, price_eur: 1.29, delivery_days: [2, 4] },
      { name: 'Lettre verte', max_weight_g: 100, price_eur: 2.58, delivery_days: [2, 4] },
    ],
    international: {
      zones: [
        {
          name: 'Europe',
          countries: ['DE', 'ES', 'GB'],
          rates: [
            { name: 'Lettre internationale', max_weight_g: 20, price_eur: 1.80, delivery_days: [3, 7] },
            { name: 'Lettre internationale', max_weight_g: 100, price_eur: 3.60, delivery_days: [3, 7] },
          ],
        },
      ],
    },
  },
  parcels: {
    domestic: [
      { name: 'Colissimo', max_weight_kg: 1, price_eur: 6.40, delivery_days: [2, 3], tracking: true },
      { name: 'Colissimo', max_weight_kg: 5, price_eur: 8.95, delivery_days: [2, 3], tracking: true },
    ],
    international: {
      zones: [
        {
          name: 'Europe',
          countries: ['DE', 'ES', 'GB'],
          rates: [
            { name: 'Colissimo International', max_weight_kg: 1, price_eur: 9.50, delivery_days: [3, 5], tracking: true },
            { name: 'Colissimo International', max_weight_kg: 5, price_eur: 18.50, delivery_days: [3, 5], tracking: true },
          ],
        },
      ],
    },
  },
};

const deutschePost: OperatorData = {
  operator: {
    id: 'deutsche-post-de',
    name: 'Deutsche Post',
    country: 'DE',
    currency: 'EUR',
    website: 'https://www.deutschepost.de',
    logo: 'deutsche-post-de.svg',
    last_updated: '2026-04-01',
  },
  letters: {
    domestic: [
      { name: 'Standardbrief', max_weight_g: 20, price_eur: 0.95, delivery_days: [1, 2] },
      { name: 'Großbrief', max_weight_g: 500, price_eur: 1.80, delivery_days: [1, 2] },
    ],
    international: {
      zones: [
        {
          name: 'Europe',
          countries: ['FR', 'ES', 'GB'],
          rates: [
            { name: 'Standardbrief International', max_weight_g: 20, price_eur: 1.10, delivery_days: [2, 5] },
            { name: 'Großbrief International', max_weight_g: 500, price_eur: 3.70, delivery_days: [2, 5] },
          ],
        },
      ],
    },
  },
  parcels: {
    domestic: [
      { name: 'DHL Paket', max_weight_kg: 5, price_eur: 6.99, delivery_days: [1, 2], tracking: true },
      { name: 'DHL Paket', max_weight_kg: 10, price_eur: 9.49, delivery_days: [1, 2], tracking: true },
    ],
    international: {
      zones: [
        {
          name: 'EU',
          countries: ['FR', 'ES'],
          rates: [
            { name: 'DHL Paket International', max_weight_kg: 5, price_eur: 13.99, delivery_days: [3, 7], tracking: true },
          ],
        },
        {
          name: 'Europe hors UE',
          countries: ['GB'],
          rates: [
            { name: 'DHL Paket International', max_weight_kg: 5, price_eur: 18.99, delivery_days: [4, 10], tracking: true },
          ],
        },
      ],
    },
  },
};

const operators = [laPoste, deutschePost];

describe('comparison engine', () => {
  describe('domestic letter comparison', () => {
    it('finds the correct domestic letter rate for a specific country', () => {
      const params: SearchParams = { type: 'letter', weight: 20, origin: 'FR', destination: 'domestic' };
      const results = compare(operators, params);

      expect(results).toHaveLength(1);
      expect(results[0].operator.id).toBe('la-poste-fr');
      expect(results[0].priceEur).toBe(1.29);
      expect(results[0].route.isDomestic).toBe(true);
    });

    it('compares domestic letter rates across all countries', () => {
      const params: SearchParams = { type: 'letter', weight: 20, origin: 'all', destination: 'domestic' };
      const results = compare(operators, params);

      expect(results).toHaveLength(2);
      // Sorted by price: Deutsche Post (0.95) < La Poste (1.29)
      expect(results[0].operator.id).toBe('deutsche-post-de');
      expect(results[0].priceEur).toBe(0.95);
      expect(results[0].isBestPrice).toBe(true);
      expect(results[1].operator.id).toBe('la-poste-fr');
      expect(results[1].priceEur).toBe(1.29);
      expect(results[1].isBestPrice).toBe(false);
    });
  });

  describe('international letter comparison', () => {
    it('finds the correct international rate for FR to DE', () => {
      const params: SearchParams = { type: 'letter', weight: 20, origin: 'FR', destination: 'DE' };
      const results = compare(operators, params);

      expect(results).toHaveLength(1);
      expect(results[0].operator.id).toBe('la-poste-fr');
      expect(results[0].priceEur).toBe(1.80);
      expect(results[0].route.isDomestic).toBe(false);
    });

    it('shows rates from multiple operators when destination is reachable', () => {
      const params: SearchParams = { type: 'letter', weight: 20, origin: 'all', destination: 'ES' };
      const results = compare(operators, params);

      expect(results).toHaveLength(2);
      // Deutsche Post (1.10) < La Poste (1.80)
      expect(results[0].operator.id).toBe('deutsche-post-de');
      expect(results[0].priceEur).toBe(1.10);
      expect(results[1].operator.id).toBe('la-poste-fr');
      expect(results[1].priceEur).toBe(1.80);
    });
  });

  describe('weight tier matching', () => {
    it('finds the correct weight tier (smallest tier >= weight)', () => {
      const params: SearchParams = { type: 'letter', weight: 50, origin: 'FR', destination: 'domestic' };
      const results = compare(operators, params);

      expect(results).toHaveLength(1);
      expect(results[0].priceEur).toBe(2.58); // 100g tier
    });

    it('excludes operators when weight exceeds all tiers', () => {
      const params: SearchParams = { type: 'letter', weight: 300, origin: 'FR', destination: 'domestic' };
      const results = compare(operators, params);

      // La Poste max is 100g domestic, Deutsche Post max is 500g
      expect(results).toHaveLength(0);
    });

    it('handles exact weight match on tier boundary', () => {
      const params: SearchParams = { type: 'letter', weight: 100, origin: 'FR', destination: 'domestic' };
      const results = compare(operators, params);

      expect(results).toHaveLength(1);
      expect(results[0].priceEur).toBe(2.58); // exactly 100g tier
    });
  });

  describe('parcel comparison', () => {
    it('compares domestic parcel rates', () => {
      const params: SearchParams = { type: 'parcel', weight: 1, origin: 'all', destination: 'domestic' };
      const results = compare(operators, params);

      expect(results).toHaveLength(2);
      // Deutsche Post DHL Paket 5kg (6.99) > La Poste Colissimo 1kg (6.40)
      expect(results[0].operator.id).toBe('la-poste-fr');
      expect(results[0].priceEur).toBe(6.40);
    });

    it('handles international parcel with zone-based routing', () => {
      const params: SearchParams = { type: 'parcel', weight: 1, origin: 'DE', destination: 'GB' };
      const results = compare(operators, params);

      expect(results).toHaveLength(1);
      expect(results[0].operator.id).toBe('deutsche-post-de');
      expect(results[0].priceEur).toBe(18.99); // Europe hors UE zone
    });

    it('includes tracking info in results', () => {
      const params: SearchParams = { type: 'parcel', weight: 1, origin: 'FR', destination: 'domestic' };
      const results = compare(operators, params);

      expect(results[0].tracking).toBe(true);
    });
  });

  describe('edge cases', () => {
    it('returns empty array when no operators cover the destination', () => {
      const params: SearchParams = { type: 'parcel', weight: 1, origin: 'FR', destination: 'JP' };
      const results = compare(operators, params);

      expect(results).toHaveLength(0);
    });

    it('handles origin=all with destination=specific correctly', () => {
      // When origin=all and destination=FR, Deutsche Post shows international,
      // La Poste shows domestic (origin=FR, dest=FR)
      const params: SearchParams = { type: 'letter', weight: 20, origin: 'all', destination: 'FR' };
      const results = compare(operators, params);

      // La Poste: origin=FR, dest=FR => domestic rate
      // Deutsche Post: origin=DE, dest=FR => international rate
      expect(results).toHaveLength(2);
    });
  });
});
```

**Commit:** `test: add comparison engine test suite`

---

## Step 13 — Comparison engine implementation

**File:** `src/lib/comparison-engine.ts`

```typescript
// src/lib/comparison-engine.ts
import type {
  OperatorData,
  SearchParams,
  ComparisonResult,
  LetterRate,
  ParcelRate,
  DeliveryDays,
} from './types';

/**
 * Find the applicable letter rate for a given weight.
 * Returns the first tier where max_weight_g >= weight.
 * Rates are assumed to be sorted by max_weight ascending in YAML.
 */
function findLetterRate(
  rates: LetterRate[],
  weightG: number,
): LetterRate | null {
  const applicable = rates.find((r) => r.max_weight_g >= weightG);
  return applicable ?? null;
}

/**
 * Find the applicable parcel rate for a given weight.
 * Returns the first tier where max_weight_kg >= weight.
 */
function findParcelRate(
  rates: ParcelRate[],
  weightKg: number,
): ParcelRate | null {
  const applicable = rates.find((r) => r.max_weight_kg >= weightKg);
  return applicable ?? null;
}

/**
 * Look up the rate for a specific operator given search params.
 * Returns null if the operator cannot serve this route/weight.
 */
function findRate(
  operator: OperatorData,
  params: SearchParams,
  originCountry: string,
): {
  productName: string;
  priceEur: number;
  deliveryDays: DeliveryDays;
  tracking: boolean;
  isDomestic: boolean;
} | null {
  const destCountry = params.destination;
  const isDomestic =
    destCountry === 'domestic' || destCountry === originCountry;

  if (params.type === 'letter') {
    if (isDomestic) {
      const rate = findLetterRate(operator.letters.domestic, params.weight);
      if (!rate) return null;
      return {
        productName: rate.name,
        priceEur: rate.price_eur,
        deliveryDays: rate.delivery_days,
        tracking: false,
        isDomestic: true,
      };
    } else {
      // Find the zone containing the destination
      const zone = operator.letters.international.zones.find((z) =>
        z.countries.includes(destCountry),
      );
      if (!zone) return null;
      const rate = findLetterRate(zone.rates, params.weight);
      if (!rate) return null;
      return {
        productName: rate.name,
        priceEur: rate.price_eur,
        deliveryDays: rate.delivery_days,
        tracking: false,
        isDomestic: false,
      };
    }
  } else {
    // parcel
    if (isDomestic) {
      const rate = findParcelRate(operator.parcels.domestic, params.weight);
      if (!rate) return null;
      return {
        productName: rate.name,
        priceEur: rate.price_eur,
        deliveryDays: rate.delivery_days,
        tracking: rate.tracking,
        isDomestic: true,
      };
    } else {
      const zone = operator.parcels.international.zones.find((z) =>
        z.countries.includes(destCountry),
      );
      if (!zone) return null;
      const rate = findParcelRate(zone.rates, params.weight);
      if (!rate) return null;
      return {
        productName: rate.name,
        priceEur: rate.price_eur,
        deliveryDays: rate.delivery_days,
        tracking: rate.tracking,
        isDomestic: false,
      };
    }
  }
}

/**
 * Main comparison function. Takes all operators and search params,
 * returns results sorted by price ascending with isBestPrice marked.
 */
export function compare(
  operators: OperatorData[],
  params: SearchParams,
): ComparisonResult[] {
  const results: ComparisonResult[] = [];

  for (const op of operators) {
    const originCountry = op.operator.country;

    // If user selected a specific origin, skip operators from other countries
    if (params.origin !== 'all' && originCountry !== params.origin) {
      continue;
    }

    // Determine the effective destination for this operator
    const effectiveDestination =
      params.destination === 'domestic' ? originCountry : params.destination;

    const rate = findRate(op, params, originCountry);
    if (!rate) continue;

    results.push({
      operator: op.operator,
      productName: rate.productName,
      priceEur: rate.priceEur,
      deliveryDays: rate.deliveryDays,
      tracking: rate.tracking,
      isBestPrice: false, // set below
      route: {
        origin: originCountry,
        destination: effectiveDestination,
        isDomestic: rate.isDomestic,
      },
    });
  }

  // Sort by price ascending
  results.sort((a, b) => a.priceEur - b.priceEur);

  // Mark best price
  if (results.length > 0) {
    results[0].isBestPrice = true;
  }

  return results;
}
```

Run tests: `npm test`

**Commit:** `feat: implement comparison engine with rate lookup and sorting`

---

## Step 14 — Vitest config

**File:** `vitest.config.ts`

```typescript
// vitest.config.ts
import { defineConfig } from 'vitest/config';
import path from 'node:path';

export default defineConfig({
  test: {
    globals: false,
    environment: 'node',
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
      '@data': path.resolve(__dirname, 'data'),
    },
  },
});
```

**Commit:** `chore: add Vitest configuration`

---

## Step 15 — Schema validation script

**File:** `scripts/validate-data.ts`

```typescript
// scripts/validate-data.ts
import Ajv from 'ajv';
import addFormats from 'ajv-formats';
import fs from 'node:fs';
import path from 'node:path';
import yaml from 'js-yaml';

const DATA_DIR = path.resolve(process.cwd(), 'data');

function main() {
  const ajv = new Ajv({ allErrors: true, strict: true });
  addFormats(ajv);

  // Load schema
  const schemaPath = path.join(DATA_DIR, 'schema', 'operator.schema.json');
  const schema = JSON.parse(fs.readFileSync(schemaPath, 'utf-8'));
  const validate = ajv.compile(schema);

  // Find all operator YAML files
  const operatorsDir = path.join(DATA_DIR, 'operators');
  const files = fs.readdirSync(operatorsDir).filter((f) => f.endsWith('.yaml'));

  let hasErrors = false;

  for (const file of files) {
    const filePath = path.join(operatorsDir, file);
    const content = fs.readFileSync(filePath, 'utf-8');
    const data = yaml.load(content);

    const valid = validate(data);
    if (!valid) {
      hasErrors = true;
      console.error(`\n❌ ${file}:`);
      for (const err of validate.errors ?? []) {
        console.error(`   ${err.instancePath} ${err.message}`);
      }
    } else {
      console.log(`✅ ${file}`);
    }
  }

  if (hasErrors) {
    console.error('\nValidation failed.');
    process.exit(1);
  } else {
    console.log(`\nAll ${files.length} operator files valid.`);
  }
}

main();
```

**File:** `tests/validate-schema.test.ts`

```typescript
// tests/validate-schema.test.ts
import { describe, it, expect } from 'vitest';
import Ajv from 'ajv';
import addFormats from 'ajv-formats';
import fs from 'node:fs';
import path from 'node:path';
import yaml from 'js-yaml';

const DATA_DIR = path.resolve(process.cwd(), 'data');

describe('operator YAML schema validation', () => {
  const ajv = new Ajv({ allErrors: true, strict: true });
  addFormats(ajv);

  const schemaPath = path.join(DATA_DIR, 'schema', 'operator.schema.json');
  const schema = JSON.parse(fs.readFileSync(schemaPath, 'utf-8'));
  const validate = ajv.compile(schema);

  const operatorsDir = path.join(DATA_DIR, 'operators');
  const files = fs.readdirSync(operatorsDir).filter((f) => f.endsWith('.yaml'));

  for (const file of files) {
    it(`validates ${file}`, () => {
      const content = fs.readFileSync(path.join(operatorsDir, file), 'utf-8');
      const data = yaml.load(content);
      const valid = validate(data);
      if (!valid) {
        const errors = validate.errors?.map(
          (e) => `${e.instancePath} ${e.message}`,
        );
        expect(valid, `Schema errors:\n${errors?.join('\n')}`).toBe(true);
      }
      expect(valid).toBe(true);
    });
  }
});
```

Run: `npm test` and `npm run validate`

**Commit:** `feat: add schema validation script and tests`

---

## Step 16 — i18n translation files

**File:** `src/i18n/fr.json`

```json
{
  "site": {
    "title": "PostCompare.eu",
    "tagline": "Comparez les tarifs postaux en Europe",
    "description": "Comparateur gratuit et indépendant des tarifs postaux européens. Trouvez le meilleur prix pour vos lettres et colis."
  },
  "nav": {
    "home": "Comparateur",
    "about": "À propos",
    "contribute": "Contribuer"
  },
  "form": {
    "type": "Type d'envoi",
    "letter": "Lettre",
    "parcel": "Colis",
    "weight": "Poids",
    "weight_grams": "Poids (grammes)",
    "weight_kg": "Poids (kg)",
    "origin": "Pays d'origine",
    "destination": "Pays de destination",
    "all_countries": "Tous les pays",
    "domestic": "National (même pays)",
    "compare": "Comparer",
    "modify": "Modifier"
  },
  "results": {
    "title": "Résultats",
    "no_results": "Aucun résultat trouvé pour cette recherche.",
    "best_price": "Meilleur prix",
    "delivery": "Livraison",
    "days": "jours ouvrables",
    "tracking": "Suivi",
    "tracking_yes": "Oui",
    "tracking_no": "Non",
    "results_count": "{count} résultat(s)",
    "price": "Prix",
    "see_details": "Voir les tarifs",
    "domestic_route": "National",
    "from": "De",
    "to": "Vers"
  },
  "operator": {
    "title": "Tarifs {name}",
    "website": "Site officiel",
    "last_updated": "Dernière mise à jour",
    "domestic_letters": "Lettres nationales",
    "international_letters": "Lettres internationales",
    "domestic_parcels": "Colis nationaux",
    "international_parcels": "Colis internationaux",
    "product": "Produit",
    "max_weight": "Poids max",
    "price": "Prix (EUR)",
    "delivery": "Délai",
    "zone": "Zone",
    "countries": "Pays"
  },
  "about": {
    "title": "À propos de PostCompare.eu",
    "intro": "PostCompare.eu est un comparateur gratuit et indépendant des tarifs postaux en Europe.",
    "methodology_title": "Méthodologie",
    "methodology_text": "Les prix sont collectés manuellement depuis les sites officiels des postes nationales. Les prix des pays hors zone euro sont convertis en EUR au taux de change du jour de la collecte.",
    "neutrality_title": "Neutralité",
    "neutrality_text": "Ce site est purement informatif. Nous n'avons aucune relation commerciale avec les opérateurs postaux listés.",
    "open_source_title": "Open Source",
    "open_source_text": "Le code source et les données sont disponibles sur GitHub. Les contributions sont bienvenues."
  },
  "contribute": {
    "title": "Contribuer à PostCompare.eu",
    "intro": "Les données de PostCompare.eu sont maintenues par la communauté. Vous pouvez aider à garder les tarifs à jour.",
    "report_title": "Signaler un prix incorrect",
    "report_text": "Ouvrez une Issue GitHub en précisant l'opérateur, le produit et le prix correct.",
    "fix_title": "Corriger directement",
    "fix_text": "Modifiez le fichier YAML de l'opérateur concerné et ouvrez une Pull Request.",
    "steps_title": "Comment contribuer",
    "step1": "Forkez le dépôt GitHub",
    "step2": "Modifiez le fichier YAML dans data/operators/",
    "step3": "Vérifiez que le schéma est valide : npm run validate",
    "step4": "Ouvrez une Pull Request avec la source de vos données"
  },
  "footer": {
    "tagline": "Comparateur indépendant de tarifs postaux européens",
    "open_source": "Projet open source",
    "disclaimer": "Les prix affichés sont indicatifs et peuvent être inexacts. Vérifiez toujours sur le site officiel de l'opérateur."
  }
}
```

**File:** `src/i18n/en.json`

```json
{
  "site": {
    "title": "PostCompare.eu",
    "tagline": "Compare postal rates across Europe",
    "description": "Free and independent European postal rate comparator. Find the best price for your letters and parcels."
  },
  "nav": {
    "home": "Comparator",
    "about": "About",
    "contribute": "Contribute"
  },
  "form": {
    "type": "Shipment type",
    "letter": "Letter",
    "parcel": "Parcel",
    "weight": "Weight",
    "weight_grams": "Weight (grams)",
    "weight_kg": "Weight (kg)",
    "origin": "Origin country",
    "destination": "Destination country",
    "all_countries": "All countries",
    "domestic": "Domestic (same country)",
    "compare": "Compare",
    "modify": "Modify"
  },
  "results": {
    "title": "Results",
    "no_results": "No results found for this search.",
    "best_price": "Best price",
    "delivery": "Delivery",
    "days": "business days",
    "tracking": "Tracking",
    "tracking_yes": "Yes",
    "tracking_no": "No",
    "results_count": "{count} result(s)",
    "price": "Price",
    "see_details": "View rates",
    "domestic_route": "Domestic",
    "from": "From",
    "to": "To"
  },
  "operator": {
    "title": "{name} rates",
    "website": "Official website",
    "last_updated": "Last updated",
    "domestic_letters": "Domestic letters",
    "international_letters": "International letters",
    "domestic_parcels": "Domestic parcels",
    "international_parcels": "International parcels",
    "product": "Product",
    "max_weight": "Max weight",
    "price": "Price (EUR)",
    "delivery": "Delivery time",
    "zone": "Zone",
    "countries": "Countries"
  },
  "about": {
    "title": "About PostCompare.eu",
    "intro": "PostCompare.eu is a free and independent comparator for postal rates across Europe.",
    "methodology_title": "Methodology",
    "methodology_text": "Prices are collected manually from national postal services' official websites. Prices from non-eurozone countries are converted to EUR at the exchange rate on the date of collection.",
    "neutrality_title": "Neutrality",
    "neutrality_text": "This site is purely informational. We have no commercial relationship with any of the listed postal operators.",
    "open_source_title": "Open Source",
    "open_source_text": "The source code and data are available on GitHub. Contributions are welcome."
  },
  "contribute": {
    "title": "Contribute to PostCompare.eu",
    "intro": "PostCompare.eu data is maintained by the community. You can help keep rates up to date.",
    "report_title": "Report an incorrect price",
    "report_text": "Open a GitHub Issue specifying the operator, product, and correct price.",
    "fix_title": "Fix it directly",
    "fix_text": "Edit the relevant operator's YAML file and open a Pull Request.",
    "steps_title": "How to contribute",
    "step1": "Fork the GitHub repository",
    "step2": "Edit the YAML file in data/operators/",
    "step3": "Verify the schema is valid: npm run validate",
    "step4": "Open a Pull Request with the source of your data"
  },
  "footer": {
    "tagline": "Independent European postal rate comparator",
    "open_source": "Open source project",
    "disclaimer": "Prices shown are indicative and may be inaccurate. Always verify on the operator's official website."
  }
}
```

**File:** `src/i18n/de.json`

```json
{
  "site": {
    "title": "PostCompare.eu",
    "tagline": "Postgebühren in Europa vergleichen",
    "description": "Kostenloser und unabhängiger Vergleich europäischer Postgebühren. Finden Sie den besten Preis für Ihre Briefe und Pakete."
  },
  "nav": {
    "home": "Vergleich",
    "about": "Über uns",
    "contribute": "Mitmachen"
  },
  "form": {
    "type": "Sendungsart",
    "letter": "Brief",
    "parcel": "Paket",
    "weight": "Gewicht",
    "weight_grams": "Gewicht (Gramm)",
    "weight_kg": "Gewicht (kg)",
    "origin": "Herkunftsland",
    "destination": "Zielland",
    "all_countries": "Alle Länder",
    "domestic": "Inland (gleiches Land)",
    "compare": "Vergleichen",
    "modify": "Ändern"
  },
  "results": {
    "title": "Ergebnisse",
    "no_results": "Keine Ergebnisse für diese Suche gefunden.",
    "best_price": "Bester Preis",
    "delivery": "Lieferung",
    "days": "Werktage",
    "tracking": "Sendungsverfolgung",
    "tracking_yes": "Ja",
    "tracking_no": "Nein",
    "results_count": "{count} Ergebnis(se)",
    "price": "Preis",
    "see_details": "Tarife anzeigen",
    "domestic_route": "Inland",
    "from": "Von",
    "to": "Nach"
  },
  "operator": {
    "title": "Tarife {name}",
    "website": "Offizielle Website",
    "last_updated": "Zuletzt aktualisiert",
    "domestic_letters": "Inlandsbriefe",
    "international_letters": "Internationale Briefe",
    "domestic_parcels": "Inlandspakete",
    "international_parcels": "Internationale Pakete",
    "product": "Produkt",
    "max_weight": "Maximalgewicht",
    "price": "Preis (EUR)",
    "delivery": "Lieferzeit",
    "zone": "Zone",
    "countries": "Länder"
  },
  "about": {
    "title": "Über PostCompare.eu",
    "intro": "PostCompare.eu ist ein kostenloser und unabhängiger Vergleich der Postgebühren in Europa.",
    "methodology_title": "Methodik",
    "methodology_text": "Die Preise werden manuell von den offiziellen Websites der nationalen Postdienste erhoben. Preise aus Nicht-Eurozone-Ländern werden zum Wechselkurs am Tag der Erhebung in EUR umgerechnet.",
    "neutrality_title": "Neutralität",
    "neutrality_text": "Diese Website ist rein informativ. Wir haben keine geschäftliche Beziehung zu den aufgelisteten Postbetreibern.",
    "open_source_title": "Open Source",
    "open_source_text": "Der Quellcode und die Daten sind auf GitHub verfügbar. Beiträge sind willkommen."
  },
  "contribute": {
    "title": "Zu PostCompare.eu beitragen",
    "intro": "Die Daten von PostCompare.eu werden von der Community gepflegt. Sie können helfen, die Tarife aktuell zu halten.",
    "report_title": "Einen falschen Preis melden",
    "report_text": "Öffnen Sie ein GitHub Issue mit Angabe des Betreibers, Produkts und korrekten Preises.",
    "fix_title": "Direkt korrigieren",
    "fix_text": "Bearbeiten Sie die YAML-Datei des betreffenden Betreibers und öffnen Sie einen Pull Request.",
    "steps_title": "So können Sie beitragen",
    "step1": "Forken Sie das GitHub-Repository",
    "step2": "Bearbeiten Sie die YAML-Datei in data/operators/",
    "step3": "Überprüfen Sie die Schema-Validierung: npm run validate",
    "step4": "Öffnen Sie einen Pull Request mit der Quelle Ihrer Daten"
  },
  "footer": {
    "tagline": "Unabhängiger europäischer Postgebührenvergleich",
    "open_source": "Open-Source-Projekt",
    "disclaimer": "Die angezeigten Preise sind indikativ und können ungenau sein. Überprüfen Sie immer auf der offiziellen Website des Betreibers."
  }
}
```

**Commit:** `feat: add i18n translation files for FR, EN, DE`

---

## Step 17 — i18n utility module

**File:** `src/i18n/utils.ts`

```typescript
// src/i18n/utils.ts
import type { Lang } from '../lib/types';
import fr from './fr.json';
import en from './en.json';
import de from './de.json';

const translations: Record<Lang, Record<string, unknown>> = { fr, en, de };

/**
 * Get a translated string by dot-notation key.
 * Example: t('fr', 'form.compare') => "Comparer"
 * Supports {placeholder} replacement via the params argument.
 */
export function t(
  lang: Lang,
  key: string,
  params?: Record<string, string | number>,
): string {
  const keys = key.split('.');
  let value: unknown = translations[lang];

  for (const k of keys) {
    if (value && typeof value === 'object' && k in value) {
      value = (value as Record<string, unknown>)[k];
    } else {
      console.warn(`Missing translation: ${lang}.${key}`);
      return key;
    }
  }

  if (typeof value !== 'string') return key;

  if (params) {
    return value.replace(/\{(\w+)\}/g, (_, name) =>
      params[name] !== undefined ? String(params[name]) : `{${name}}`,
    );
  }

  return value;
}

/**
 * Get the language from a URL path (e.g., /fr/about => 'fr')
 */
export function getLangFromPath(path: string): Lang {
  const match = path.match(/^\/(fr|en|de)/);
  return (match?.[1] as Lang) ?? 'fr';
}

/**
 * Build a localized path
 */
export function localePath(lang: Lang, path: string): string {
  return `/${lang}${path}`;
}

/** All supported languages */
export const languages: Lang[] = ['fr', 'en', 'de'];

/** Language display names */
export const languageNames: Record<Lang, string> = {
  fr: 'Français',
  en: 'English',
  de: 'Deutsch',
};
```

**Commit:** `feat: add i18n utility functions`

---

## Step 18 — Global CSS with postal theme

**File:** `src/styles/global.css`

```css
/* src/styles/global.css */

/* ===== CSS Custom Properties ===== */
:root {
  /* Postal theme colors */
  --color-cream: #fdf6e3;
  --color-cream-light: #fffef7;
  --color-postal-red: #c0392b;
  --color-postal-red-dark: #a93226;
  --color-gold: #8b6914;
  --color-gold-light: #c4a35a;
  --color-kraft: #d4a853;
  --color-text: #2c1810;
  --color-text-secondary: #5d4e37;
  --color-border: #c4a35a;
  --color-border-light: #e0d0a0;
  --color-white: #ffffff;
  --color-success: #27ae60;

  /* Typography */
  --font-heading: Georgia, 'Times New Roman', serif;
  --font-body: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
    sans-serif;

  /* Spacing */
  --space-xs: 0.25rem;
  --space-sm: 0.5rem;
  --space-md: 1rem;
  --space-lg: 1.5rem;
  --space-xl: 2rem;
  --space-2xl: 3rem;

  /* Layout */
  --sidebar-width: 35%;
  --max-width: 1400px;
  --header-height: 64px;

  /* Borders */
  --border-dashed: 2px dashed var(--color-border);
  --border-solid: 1px solid var(--color-border-light);
  --radius: 6px;
  --radius-lg: 12px;

  /* Shadows */
  --shadow-sm: 0 1px 3px rgba(44, 24, 16, 0.08);
  --shadow-md: 0 4px 12px rgba(44, 24, 16, 0.1);
  --shadow-lg: 0 8px 24px rgba(44, 24, 16, 0.12);
}

/* ===== Reset ===== */
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: var(--font-body);
  color: var(--color-text);
  background-color: var(--color-cream);
  line-height: 1.6;
  min-height: 100vh;
}

/* ===== Typography ===== */
h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-heading);
  font-weight: 700;
  line-height: 1.3;
  color: var(--color-text);
}

h1 { font-size: 2rem; }
h2 { font-size: 1.5rem; }
h3 { font-size: 1.25rem; }

a {
  color: var(--color-postal-red);
  text-decoration: none;
  transition: color 0.2s;
}
a:hover {
  color: var(--color-postal-red-dark);
  text-decoration: underline;
}

/* ===== Header ===== */
.header {
  background-color: var(--color-postal-red);
  color: var(--color-white);
  height: var(--header-height);
  display: flex;
  align-items: center;
  padding: 0 var(--space-lg);
  position: sticky;
  top: 0;
  z-index: 100;
  box-shadow: var(--shadow-md);
}

.header__inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  max-width: var(--max-width);
  margin: 0 auto;
}

.header__logo {
  font-family: var(--font-heading);
  font-size: 1.4rem;
  color: var(--color-white);
  font-weight: 700;
  display: flex;
  align-items: center;
  gap: var(--space-sm);
}
.header__logo:hover {
  color: var(--color-white);
  text-decoration: none;
}

.header__nav {
  display: flex;
  align-items: center;
  gap: var(--space-lg);
}

.header__nav a {
  color: rgba(255, 255, 255, 0.9);
  font-size: 0.95rem;
  transition: color 0.2s;
}
.header__nav a:hover {
  color: var(--color-white);
  text-decoration: none;
}

.header__burger {
  display: none;
  background: none;
  border: none;
  color: var(--color-white);
  font-size: 1.5rem;
  cursor: pointer;
}

/* ===== Main Layout ===== */
.main-layout {
  display: flex;
  max-width: var(--max-width);
  margin: 0 auto;
  min-height: calc(100vh - var(--header-height) - 200px);
}

.sidebar {
  width: var(--sidebar-width);
  padding: var(--space-xl);
  border-right: var(--border-dashed);
  background-color: var(--color-cream-light);
  position: sticky;
  top: var(--header-height);
  height: fit-content;
  max-height: calc(100vh - var(--header-height));
  overflow-y: auto;
}

.main-content {
  flex: 1;
  padding: var(--space-xl);
}

/* ===== Form Styles ===== */
.form-group {
  margin-bottom: var(--space-lg);
}

.form-label {
  display: block;
  font-family: var(--font-heading);
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--color-gold);
  margin-bottom: var(--space-xs);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.form-select,
.form-input {
  width: 100%;
  padding: var(--space-sm) var(--space-md);
  border: var(--border-solid);
  border-radius: var(--radius);
  font-size: 1rem;
  font-family: var(--font-body);
  background-color: var(--color-white);
  color: var(--color-text);
  transition: border-color 0.2s;
}

.form-select:focus,
.form-input:focus {
  outline: none;
  border-color: var(--color-gold);
  box-shadow: 0 0 0 3px rgba(139, 105, 20, 0.15);
}

.type-toggle {
  display: flex;
  gap: var(--space-sm);
}

.type-toggle__btn {
  flex: 1;
  padding: var(--space-sm) var(--space-md);
  border: var(--border-solid);
  border-radius: var(--radius);
  background: var(--color-white);
  font-family: var(--font-body);
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.2s;
  color: var(--color-text-secondary);
}

.type-toggle__btn--active {
  background: var(--color-postal-red);
  color: var(--color-white);
  border-color: var(--color-postal-red);
}

.btn-compare {
  width: 100%;
  padding: var(--space-md) var(--space-lg);
  background-color: var(--color-postal-red);
  color: var(--color-white);
  border: none;
  border-radius: var(--radius);
  font-family: var(--font-heading);
  font-size: 1.1rem;
  font-weight: 700;
  cursor: pointer;
  transition: background-color 0.2s;
  margin-top: var(--space-md);
}

.btn-compare:hover {
  background-color: var(--color-postal-red-dark);
}

/* ===== Result Cards ===== */
.results-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-lg);
}

.results-count {
  color: var(--color-text-secondary);
  font-size: 0.9rem;
}

.result-card {
  background: var(--color-white);
  border: var(--border-solid);
  border-radius: var(--radius-lg);
  padding: var(--space-lg);
  margin-bottom: var(--space-md);
  display: flex;
  align-items: center;
  gap: var(--space-lg);
  transition: box-shadow 0.2s;
  box-shadow: var(--shadow-sm);
}

.result-card:hover {
  box-shadow: var(--shadow-md);
}

.result-card--best {
  border: 2px solid var(--color-gold);
  position: relative;
}

.result-card__badge {
  position: absolute;
  top: -10px;
  left: var(--space-lg);
  background: var(--color-gold);
  color: var(--color-white);
  font-size: 0.75rem;
  font-weight: 700;
  padding: 2px var(--space-sm);
  border-radius: var(--radius);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.result-card__operator {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  min-width: 180px;
}

.result-card__flag { font-size: 1.5rem; }

.result-card__name {
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 1rem;
}

.result-card__product {
  color: var(--color-text-secondary);
  font-size: 0.85rem;
}

.result-card__details {
  flex: 1;
  display: flex;
  gap: var(--space-xl);
}

.result-card__route {
  font-size: 0.85rem;
  color: var(--color-text-secondary);
}

.result-card__delivery { font-size: 0.9rem; }

.result-card__tracking {
  font-size: 0.85rem;
  color: var(--color-text-secondary);
}

.result-card__price {
  font-family: var(--font-heading);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--color-text);
  text-align: right;
  min-width: 100px;
}

.result-card--best .result-card__price {
  color: var(--color-postal-red);
}

.result-card__price-unit {
  font-size: 0.85rem;
  font-weight: 400;
  color: var(--color-text-secondary);
}

.result-card__link {
  font-size: 0.85rem;
  color: var(--color-postal-red);
  margin-top: var(--space-xs);
}

/* ===== Mobile Summary Pills ===== */
.mobile-summary {
  display: none;
  background: var(--color-cream-light);
  border-bottom: var(--border-dashed);
  padding: var(--space-md) var(--space-lg);
}

.mobile-summary__pills {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-sm);
  align-items: center;
}

.pill {
  background: var(--color-white);
  border: var(--border-solid);
  border-radius: 20px;
  padding: var(--space-xs) var(--space-md);
  font-size: 0.85rem;
  color: var(--color-text);
}

.mobile-summary__modify {
  background: none;
  border: none;
  color: var(--color-postal-red);
  font-size: 0.85rem;
  cursor: pointer;
  padding: var(--space-xs) var(--space-sm);
  font-weight: 600;
}

/* ===== Footer ===== */
.footer {
  background: var(--color-text);
  color: rgba(255, 255, 255, 0.8);
  padding: var(--space-xl) var(--space-lg);
  text-align: center;
  font-size: 0.85rem;
}

.footer a { color: var(--color-gold-light); }

.footer__disclaimer {
  margin-top: var(--space-md);
  font-size: 0.75rem;
  color: rgba(255, 255, 255, 0.5);
}

/* ===== Language Picker ===== */
.lang-picker {
  display: flex;
  gap: var(--space-xs);
}

.lang-picker__link {
  color: rgba(255, 255, 255, 0.7);
  font-size: 0.85rem;
  padding: var(--space-xs) var(--space-sm);
  border-radius: var(--radius);
  transition: all 0.2s;
}

.lang-picker__link:hover {
  color: var(--color-white);
  text-decoration: none;
}

.lang-picker__link--active {
  color: var(--color-white);
  background: rgba(255, 255, 255, 0.15);
}

/* ===== Operator Detail Page ===== */
.operator-header {
  display: flex;
  align-items: center;
  gap: var(--space-lg);
  margin-bottom: var(--space-xl);
  padding-bottom: var(--space-lg);
  border-bottom: var(--border-dashed);
}

.operator-header__flag { font-size: 3rem; }

.operator-header__meta {
  font-size: 0.9rem;
  color: var(--color-text-secondary);
}

.rate-table {
  width: 100%;
  border-collapse: collapse;
  margin-bottom: var(--space-xl);
}

.rate-table th {
  background: var(--color-cream-light);
  font-family: var(--font-heading);
  font-size: 0.85rem;
  color: var(--color-gold);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  padding: var(--space-sm) var(--space-md);
  text-align: left;
  border-bottom: var(--border-dashed);
}

.rate-table td {
  padding: var(--space-sm) var(--space-md);
  border-bottom: var(--border-solid);
  font-size: 0.95rem;
}

.rate-table tr:hover td {
  background: var(--color-cream-light);
}

/* ===== Content Pages (About, Contribute) ===== */
.content-page {
  max-width: 800px;
  margin: 0 auto;
  padding: var(--space-2xl) var(--space-lg);
}

.content-page h1 {
  margin-bottom: var(--space-lg);
  padding-bottom: var(--space-md);
  border-bottom: var(--border-dashed);
}

.content-page h2 {
  margin-top: var(--space-xl);
  margin-bottom: var(--space-md);
  color: var(--color-gold);
}

.content-page p {
  margin-bottom: var(--space-md);
  line-height: 1.8;
}

.content-page ol {
  padding-left: var(--space-lg);
  margin-bottom: var(--space-md);
}

.content-page li {
  margin-bottom: var(--space-sm);
  line-height: 1.6;
}

.content-page code {
  background: var(--color-cream-light);
  border: var(--border-solid);
  border-radius: var(--radius);
  padding: 2px var(--space-sm);
  font-size: 0.9rem;
}

/* ===== No Results ===== */
.no-results {
  text-align: center;
  padding: var(--space-2xl);
  color: var(--color-text-secondary);
}

.no-results__icon {
  font-size: 3rem;
  margin-bottom: var(--space-md);
}

/* ===== Responsive ===== */
@media (max-width: 1199px) {
  .sidebar { width: 40%; }
}

@media (max-width: 767px) {
  .header__nav { display: none; }
  .header__nav--open {
    display: flex;
    flex-direction: column;
    position: absolute;
    top: var(--header-height);
    left: 0;
    right: 0;
    background: var(--color-postal-red);
    padding: var(--space-md) var(--space-lg);
    box-shadow: var(--shadow-md);
  }
  .header__burger { display: block; }

  .main-layout { flex-direction: column; }

  .sidebar {
    width: 100%;
    position: static;
    border-right: none;
    border-bottom: var(--border-dashed);
    max-height: none;
  }

  .sidebar--collapsed { display: none; }

  .mobile-summary {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .main-content { padding: var(--space-md); }

  .result-card {
    flex-direction: column;
    align-items: flex-start;
  }

  .result-card__details {
    flex-direction: column;
    gap: var(--space-sm);
  }

  .result-card__price {
    text-align: left;
    font-size: 1.8rem;
  }

  .result-card__operator { min-width: auto; }
}
```

**Commit:** `feat: add global CSS with postal/kraft theme and responsive design`

---

## Step 19 — Base layout

**File:** `src/layouts/BaseLayout.astro`

```astro
---
// src/layouts/BaseLayout.astro
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import type { Lang } from '../lib/types';

interface Props {
  title: string;
  description?: string;
  lang: Lang;
}

const { title, description, lang } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{title} | PostCompare.eu</title>
    {description && <meta name="description" content={description} />}
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <Header lang={lang} />
    <slot />
    <Footer lang={lang} />
  </body>
</html>
```

**Commit:** `feat: add base layout component`

---

## Step 20 — Header component

**File:** `src/components/Header.astro`

```astro
---
// src/components/Header.astro
import LanguagePicker from './LanguagePicker.astro';
import { t, localePath } from '../i18n/utils';
import type { Lang } from '../lib/types';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const currentPath = Astro.url.pathname.replace(/^\/(fr|en|de)/, '');
---

<header class="header">
  <div class="header__inner">
    <a href={localePath(lang, '/')} class="header__logo">
      ✉️ PostCompare.eu
    </a>

    <button class="header__burger" aria-label="Menu" id="burger-btn">
      ☰
    </button>

    <nav class="header__nav" id="nav-menu">
      <a href={localePath(lang, '/')}>{t(lang, 'nav.home')}</a>
      <a href={localePath(lang, '/about')}>{t(lang, 'nav.about')}</a>
      <a href={localePath(lang, '/contribute')}>{t(lang, 'nav.contribute')}</a>
      <LanguagePicker lang={lang} currentPath={currentPath} />
    </nav>
  </div>
</header>

<script>
  const burger = document.getElementById('burger-btn');
  const nav = document.getElementById('nav-menu');
  burger?.addEventListener('click', () => {
    nav?.classList.toggle('header__nav--open');
  });
</script>
```

**Commit:** `feat: add header component with navigation and burger menu`

---

## Step 21 — Language picker component

**File:** `src/components/LanguagePicker.astro`

```astro
---
// src/components/LanguagePicker.astro
import { languages, languageNames } from '../i18n/utils';
import type { Lang } from '../lib/types';

interface Props {
  lang: Lang;
  currentPath: string;
}

const { lang, currentPath } = Astro.props;
---

<div class="lang-picker">
  {
    languages.map((l) => (
      <a
        href={`/${l}${currentPath || '/'}`}
        class:list={['lang-picker__link', { 'lang-picker__link--active': l === lang }]}
      >
        {languageNames[l]}
      </a>
    ))
  }
</div>
```

**Commit:** `feat: add language picker component`

---

## Step 22 — Footer component

**File:** `src/components/Footer.astro`

```astro
---
// src/components/Footer.astro
import { t } from '../i18n/utils';
import type { Lang } from '../lib/types';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
---

<footer class="footer">
  <p>✉️ PostCompare.eu — {t(lang, 'footer.tagline')}</p>
  <p>
    <a href="https://github.com/postcompare-eu/postcompare" target="_blank" rel="noopener">
      {t(lang, 'footer.open_source')} ↗
    </a>
  </p>
  <p class="footer__disclaimer">{t(lang, 'footer.disclaimer')}</p>
</footer>
```

**Commit:** `feat: add footer component`

---

## Step 23 — Comparator form (React)

**File:** `src/components/ComparatorForm.tsx`

```tsx
// src/components/ComparatorForm.tsx
import { useState, useEffect } from 'react';
import type { SearchParams, ShipmentType, Country, Lang } from '../lib/types';
import { t } from '../i18n/utils';

interface ComparatorFormProps {
  countries: Country[];
  lang: Lang;
  onSearch: (params: SearchParams) => void;
  initialParams?: SearchParams;
}

export default function ComparatorForm({
  countries,
  lang,
  onSearch,
  initialParams,
}: ComparatorFormProps) {
  const [type, setType] = useState<ShipmentType>(initialParams?.type ?? 'letter');
  const [weight, setWeight] = useState<number>(initialParams?.weight ?? 20);
  const [origin, setOrigin] = useState<string>(initialParams?.origin ?? 'all');
  const [destination, setDestination] = useState<string>(
    initialParams?.destination ?? 'domestic',
  );

  const countryName = (code: string): string => {
    const country = countries.find((c) => c.code === code);
    if (!country) return code;
    return country[`name_${lang}` as keyof Country] as string;
  };

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    onSearch({ type, weight, origin, destination });
  };

  // Auto-search on mount with defaults
  useEffect(() => {
    onSearch({ type, weight, origin, destination });
  }, []); // eslint-disable-line react-hooks/exhaustive-deps

  return (
    <form onSubmit={handleSubmit}>
      {/* Type toggle */}
      <div className="form-group">
        <label className="form-label">{t(lang, 'form.type')}</label>
        <div className="type-toggle">
          <button
            type="button"
            className={`type-toggle__btn ${type === 'letter' ? 'type-toggle__btn--active' : ''}`}
            onClick={() => {
              setType('letter');
              setWeight(20);
            }}
          >
            ✉️ {t(lang, 'form.letter')}
          </button>
          <button
            type="button"
            className={`type-toggle__btn ${type === 'parcel' ? 'type-toggle__btn--active' : ''}`}
            onClick={() => {
              setType('parcel');
              setWeight(1);
            }}
          >
            📦 {t(lang, 'form.parcel')}
          </button>
        </div>
      </div>

      {/* Weight input */}
      <div className="form-group">
        <label className="form-label" htmlFor="weight">
          {type === 'letter' ? t(lang, 'form.weight_grams') : t(lang, 'form.weight_kg')}
        </label>
        <input
          id="weight"
          type="number"
          className="form-input"
          value={weight}
          onChange={(e) => setWeight(Number(e.target.value))}
          min={type === 'letter' ? 1 : 0.1}
          max={type === 'letter' ? 2000 : 50}
          step={type === 'letter' ? 1 : 0.1}
        />
      </div>

      {/* Origin country */}
      <div className="form-group">
        <label className="form-label" htmlFor="origin">
          {t(lang, 'form.origin')}
        </label>
        <select
          id="origin"
          className="form-select"
          value={origin}
          onChange={(e) => setOrigin(e.target.value)}
        >
          <option value="all">{t(lang, 'form.all_countries')}</option>
          {countries.map((c) => (
            <option key={c.code} value={c.code}>
              {c.flag} {countryName(c.code)}
            </option>
          ))}
        </select>
      </div>

      {/* Destination country */}
      <div className="form-group">
        <label className="form-label" htmlFor="destination">
          {t(lang, 'form.destination')}
        </label>
        <select
          id="destination"
          className="form-select"
          value={destination}
          onChange={(e) => setDestination(e.target.value)}
        >
          <option value="domestic">{t(lang, 'form.domestic')}</option>
          {countries.map((c) => (
            <option key={c.code} value={c.code}>
              {c.flag} {countryName(c.code)}
            </option>
          ))}
        </select>
      </div>

      <button type="submit" className="btn-compare">
        {t(lang, 'form.compare')}
      </button>
    </form>
  );
}
```

**Commit:** `feat: add comparator form React component`

---

## Step 24 — Result card (React)

**File:** `src/components/ResultCard.tsx`

```tsx
// src/components/ResultCard.tsx
import type { ComparisonResult, Country, Lang } from '../lib/types';
import { t, localePath } from '../i18n/utils';

interface ResultCardProps {
  result: ComparisonResult;
  countries: Country[];
  lang: Lang;
}

export default function ResultCard({ result, countries, lang }: ResultCardProps) {
  const getCountry = (code: string): Country | undefined =>
    countries.find((c) => c.code === code);

  const originCountry = getCountry(result.route.origin);
  const destCountry = getCountry(result.route.destination);

  const countryName = (country: Country | undefined): string => {
    if (!country) return '';
    return country[`name_${lang}` as keyof Country] as string;
  };

  const routeLabel = result.route.isDomestic
    ? `${t(lang, 'results.domestic_route')} (${originCountry?.flag ?? ''} ${countryName(originCountry)})`
    : `${originCountry?.flag ?? ''} ${countryName(originCountry)} → ${destCountry?.flag ?? ''} ${countryName(destCountry)}`;

  return (
    <div className={`result-card ${result.isBestPrice ? 'result-card--best' : ''}`}>
      {result.isBestPrice && (
        <span className="result-card__badge">{t(lang, 'results.best_price')}</span>
      )}

      <div className="result-card__operator">
        <span className="result-card__flag">{originCountry?.flag}</span>
        <div>
          <div className="result-card__name">{result.operator.name}</div>
          <div className="result-card__product">{result.productName}</div>
        </div>
      </div>

      <div className="result-card__details">
        <div>
          <div className="result-card__route">{routeLabel}</div>
          <div className="result-card__delivery">
            {t(lang, 'results.delivery')}: {result.deliveryDays[0]}–{result.deliveryDays[1]}{' '}
            {t(lang, 'results.days')}
          </div>
          {result.tracking !== undefined && (
            <div className="result-card__tracking">
              {t(lang, 'results.tracking')}:{' '}
              {result.tracking ? t(lang, 'results.tracking_yes') : t(lang, 'results.tracking_no')}
            </div>
          )}
        </div>
      </div>

      <div className="result-card__price">
        <div>
          {result.priceEur.toFixed(2)} <span className="result-card__price-unit">EUR</span>
        </div>
        <a
          className="result-card__link"
          href={localePath(lang, `/operator/${result.operator.id}`)}
        >
          {t(lang, 'results.see_details')} →
        </a>
      </div>
    </div>
  );
}
```

**Commit:** `feat: add result card React component`

---

## Step 25 — Results list (React)

**File:** `src/components/ResultsList.tsx`

```tsx
// src/components/ResultsList.tsx
import type { ComparisonResult, Country, Lang } from '../lib/types';
import { t } from '../i18n/utils';
import ResultCard from './ResultCard';

interface ResultsListProps {
  results: ComparisonResult[];
  countries: Country[];
  lang: Lang;
  hasSearched: boolean;
}

export default function ResultsList({
  results,
  countries,
  lang,
  hasSearched,
}: ResultsListProps) {
  if (!hasSearched) {
    return null;
  }

  if (results.length === 0) {
    return (
      <div className="no-results">
        <div className="no-results__icon">📭</div>
        <p>{t(lang, 'results.no_results')}</p>
      </div>
    );
  }

  return (
    <div>
      <div className="results-header">
        <h2>{t(lang, 'results.title')}</h2>
        <span className="results-count">
          {t(lang, 'results.results_count', { count: results.length })}
        </span>
      </div>

      {results.map((result, index) => (
        <ResultCard
          key={`${result.operator.id}-${result.route.origin}-${index}`}
          result={result}
          countries={countries}
          lang={lang}
        />
      ))}
    </div>
  );
}
```

**Commit:** `feat: add results list React component`

---

## Step 26 — Parent comparator wrapper (React)

**File:** `src/components/Comparator.tsx`

```tsx
// src/components/Comparator.tsx
import { useState, useCallback } from 'react';
import ComparatorForm from './ComparatorForm';
import ResultsList from './ResultsList';
import { compare } from '../lib/comparison-engine';
import { t } from '../i18n/utils';
import type {
  OperatorData,
  Country,
  SearchParams,
  ComparisonResult,
  Lang,
} from '../lib/types';

interface ComparatorProps {
  operators: OperatorData[];
  countries: Country[];
  lang: Lang;
}

export default function Comparator({ operators, countries, lang }: ComparatorProps) {
  const [results, setResults] = useState<ComparisonResult[]>([]);
  const [hasSearched, setHasSearched] = useState(false);
  const [currentParams, setCurrentParams] = useState<SearchParams | null>(null);
  const [mobileFormOpen, setMobileFormOpen] = useState(false);

  const handleSearch = useCallback(
    (params: SearchParams) => {
      const searchResults = compare(operators, params);
      setResults(searchResults);
      setHasSearched(true);
      setCurrentParams(params);
      setMobileFormOpen(false);
    },
    [operators],
  );

  const getCountryName = (code: string): string => {
    const country = countries.find((c) => c.code === code);
    if (!country) return code;
    return country[`name_${lang}` as keyof Country] as string;
  };

  const getCountryFlag = (code: string): string => {
    return countries.find((c) => c.code === code)?.flag ?? '';
  };

  return (
    <>
      {/* Mobile summary bar */}
      {hasSearched && currentParams && !mobileFormOpen && (
        <div className="mobile-summary">
          <div className="mobile-summary__pills">
            <span className="pill">
              {currentParams.type === 'letter' ? '✉️' : '📦'}{' '}
              {t(lang, `form.${currentParams.type}`)}
            </span>
            <span className="pill">
              {currentParams.weight}
              {currentParams.type === 'letter' ? 'g' : 'kg'}
            </span>
            <span className="pill">
              {currentParams.origin === 'all'
                ? t(lang, 'form.all_countries')
                : `${getCountryFlag(currentParams.origin)} ${getCountryName(currentParams.origin)}`}
              {' → '}
              {currentParams.destination === 'domestic'
                ? t(lang, 'form.domestic')
                : `${getCountryFlag(currentParams.destination)} ${getCountryName(currentParams.destination)}`}
            </span>
          </div>
          <button
            className="mobile-summary__modify"
            onClick={() => setMobileFormOpen(true)}
          >
            {t(lang, 'form.modify')}
          </button>
        </div>
      )}

      <div className="main-layout">
        {/* Sidebar form */}
        <aside className={`sidebar ${hasSearched && !mobileFormOpen ? 'sidebar--collapsed' : ''}`}>
          <ComparatorForm
            countries={countries}
            lang={lang}
            onSearch={handleSearch}
            initialParams={currentParams ?? undefined}
          />
        </aside>

        {/* Results */}
        <main className="main-content">
          <ResultsList
            results={results}
            countries={countries}
            lang={lang}
            hasSearched={hasSearched}
          />
        </main>
      </div>
    </>
  );
}
```

**Commit:** `feat: add parent comparator wrapper with mobile summary`

---

## Step 27 — Home page (index redirect)

**File:** `src/pages/index.astro`

```astro
---
// src/pages/index.astro
// Redirect root to default locale
return Astro.redirect('/fr/');
---
```

**Commit:** `feat: add root index page with redirect to /fr/`

---

## Step 28 — Localized home page

**File:** `src/pages/[lang]/index.astro`

```astro
---
// src/pages/[lang]/index.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import Comparator from '../../components/Comparator';
import { t } from '../../i18n/utils';
import { loadAllOperators, loadCountries } from '../../lib/data-loader';
import type { Lang } from '../../lib/types';

export function getStaticPaths() {
  return [
    { params: { lang: 'fr' } },
    { params: { lang: 'en' } },
    { params: { lang: 'de' } },
  ];
}

const lang = Astro.params.lang as Lang;
const operators = loadAllOperators();
const countries = loadCountries();
---

<BaseLayout
  title={t(lang, 'site.tagline')}
  description={t(lang, 'site.description')}
  lang={lang}
>
  <Comparator
    client:load
    operators={operators}
    countries={countries}
    lang={lang}
  />
</BaseLayout>
```

**Commit:** `feat: add localized home page with comparator`

---

## Step 29 — Operator detail page

**File:** `src/pages/[lang]/operator/[id].astro`

```astro
---
// src/pages/[lang]/operator/[id].astro
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { t, localePath } from '../../../i18n/utils';
import { loadAllOperators, loadCountries, getOperatorIds } from '../../../lib/data-loader';
import type { Lang, Country } from '../../../lib/types';

export function getStaticPaths() {
  const langs: Lang[] = ['fr', 'en', 'de'];
  const ids = getOperatorIds();

  return langs.flatMap((lang) =>
    ids.map((id) => ({
      params: { lang, id },
    })),
  );
}

const lang = Astro.params.lang as Lang;
const operatorId = Astro.params.id;

const operators = loadAllOperators();
const operator = operators.find((op) => op.operator.id === operatorId);
const countries = loadCountries();

if (!operator) {
  return Astro.redirect(localePath(lang, '/'));
}

const info = operator.operator;

const getCountry = (code: string): Country | undefined =>
  countries.find((c) => c.code === code);

const countryName = (code: string): string => {
  const c = getCountry(code);
  if (!c) return code;
  return c[`name_${lang}` as keyof Country] as string;
};

const operatorCountry = getCountry(info.country);
---

<BaseLayout
  title={t(lang, 'operator.title', { name: info.name })}
  lang={lang}
>
  <div class="content-page">
    <div class="operator-header">
      <span class="operator-header__flag">{operatorCountry?.flag}</span>
      <div>
        <h1>{info.name}</h1>
        <div class="operator-header__meta">
          {countryName(info.country)} —
          <a href={info.website} target="_blank" rel="noopener">
            {t(lang, 'operator.website')} ↗
          </a>
        </div>
        <div class="operator-header__meta">
          {t(lang, 'operator.last_updated')}: {info.last_updated}
        </div>
      </div>
    </div>

    <!-- Domestic Letters -->
    <h2>{t(lang, 'operator.domestic_letters')}</h2>
    <table class="rate-table">
      <thead>
        <tr>
          <th>{t(lang, 'operator.product')}</th>
          <th>{t(lang, 'operator.max_weight')}</th>
          <th>{t(lang, 'operator.price')}</th>
          <th>{t(lang, 'operator.delivery')}</th>
        </tr>
      </thead>
      <tbody>
        {operator.letters.domestic.map((rate) => (
          <tr>
            <td>{rate.name}</td>
            <td>{rate.max_weight_g}g</td>
            <td>{rate.price_eur.toFixed(2)} €</td>
            <td>{rate.delivery_days[0]}–{rate.delivery_days[1]} {t(lang, 'results.days')}</td>
          </tr>
        ))}
      </tbody>
    </table>

    <!-- International Letters -->
    <h2>{t(lang, 'operator.international_letters')}</h2>
    {operator.letters.international.zones.map((zone) => (
      <div>
        <h3>{t(lang, 'operator.zone')}: {zone.name}</h3>
        <p class="operator-header__meta">
          {t(lang, 'operator.countries')}: {zone.countries.map((c) => `${getCountry(c)?.flag ?? ''} ${countryName(c)}`).join(', ')}
        </p>
        <table class="rate-table">
          <thead>
            <tr>
              <th>{t(lang, 'operator.product')}</th>
              <th>{t(lang, 'operator.max_weight')}</th>
              <th>{t(lang, 'operator.price')}</th>
              <th>{t(lang, 'operator.delivery')}</th>
            </tr>
          </thead>
          <tbody>
            {zone.rates.map((rate) => (
              <tr>
                <td>{rate.name}</td>
                <td>{rate.max_weight_g}g</td>
                <td>{rate.price_eur.toFixed(2)} €</td>
                <td>{rate.delivery_days[0]}–{rate.delivery_days[1]} {t(lang, 'results.days')}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    ))}

    <!-- Domestic Parcels -->
    <h2>{t(lang, 'operator.domestic_parcels')}</h2>
    <table class="rate-table">
      <thead>
        <tr>
          <th>{t(lang, 'operator.product')}</th>
          <th>{t(lang, 'operator.max_weight')}</th>
          <th>{t(lang, 'operator.price')}</th>
          <th>{t(lang, 'operator.delivery')}</th>
          <th>{t(lang, 'results.tracking')}</th>
        </tr>
      </thead>
      <tbody>
        {operator.parcels.domestic.map((rate) => (
          <tr>
            <td>{rate.name}</td>
            <td>{rate.max_weight_kg}kg</td>
            <td>{rate.price_eur.toFixed(2)} €</td>
            <td>{rate.delivery_days[0]}–{rate.delivery_days[1]} {t(lang, 'results.days')}</td>
            <td>{rate.tracking ? t(lang, 'results.tracking_yes') : t(lang, 'results.tracking_no')}</td>
          </tr>
        ))}
      </tbody>
    </table>

    <!-- International Parcels -->
    <h2>{t(lang, 'operator.international_parcels')}</h2>
    {operator.parcels.international.zones.map((zone) => (
      <div>
        <h3>{t(lang, 'operator.zone')}: {zone.name}</h3>
        <p class="operator-header__meta">
          {t(lang, 'operator.countries')}: {zone.countries.map((c) => `${getCountry(c)?.flag ?? ''} ${countryName(c)}`).join(', ')}
        </p>
        <table class="rate-table">
          <thead>
            <tr>
              <th>{t(lang, 'operator.product')}</th>
              <th>{t(lang, 'operator.max_weight')}</th>
              <th>{t(lang, 'operator.price')}</th>
              <th>{t(lang, 'operator.delivery')}</th>
              <th>{t(lang, 'results.tracking')}</th>
            </tr>
          </thead>
          <tbody>
            {zone.rates.map((rate) => (
              <tr>
                <td>{rate.name}</td>
                <td>{rate.max_weight_kg}kg</td>
                <td>{rate.price_eur.toFixed(2)} €</td>
                <td>{rate.delivery_days[0]}–{rate.delivery_days[1]} {t(lang, 'results.days')}</td>
                <td>{rate.tracking ? t(lang, 'results.tracking_yes') : t(lang, 'results.tracking_no')}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    ))}

    <p style="margin-top: var(--space-xl); text-align: center;">
      <a href={localePath(lang, '/')}>← {t(lang, 'nav.home')}</a>
    </p>
  </div>
</BaseLayout>
```

**Commit:** `feat: add operator detail page with rate tables`

---

## Step 30 — About page

**File:** `src/pages/[lang]/about.astro`

```astro
---
// src/pages/[lang]/about.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import { t } from '../../i18n/utils';
import type { Lang } from '../../lib/types';

export function getStaticPaths() {
  return [
    { params: { lang: 'fr' } },
    { params: { lang: 'en' } },
    { params: { lang: 'de' } },
  ];
}

const lang = Astro.params.lang as Lang;
---

<BaseLayout title={t(lang, 'about.title')} lang={lang}>
  <div class="content-page">
    <h1>{t(lang, 'about.title')}</h1>

    <p>{t(lang, 'about.intro')}</p>

    <h2>{t(lang, 'about.methodology_title')}</h2>
    <p>{t(lang, 'about.methodology_text')}</p>

    <h2>{t(lang, 'about.neutrality_title')}</h2>
    <p>{t(lang, 'about.neutrality_text')}</p>

    <h2>{t(lang, 'about.open_source_title')}</h2>
    <p>{t(lang, 'about.open_source_text')}</p>
  </div>
</BaseLayout>
```

**Commit:** `feat: add about page`

---

## Step 31 — Contribute page

**File:** `src/pages/[lang]/contribute.astro`

```astro
---
// src/pages/[lang]/contribute.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import { t } from '../../i18n/utils';
import type { Lang } from '../../lib/types';

export function getStaticPaths() {
  return [
    { params: { lang: 'fr' } },
    { params: { lang: 'en' } },
    { params: { lang: 'de' } },
  ];
}

const lang = Astro.params.lang as Lang;
---

<BaseLayout title={t(lang, 'contribute.title')} lang={lang}>
  <div class="content-page">
    <h1>{t(lang, 'contribute.title')}</h1>

    <p>{t(lang, 'contribute.intro')}</p>

    <h2>{t(lang, 'contribute.report_title')}</h2>
    <p>{t(lang, 'contribute.report_text')}</p>

    <h2>{t(lang, 'contribute.fix_title')}</h2>
    <p>{t(lang, 'contribute.fix_text')}</p>

    <h2>{t(lang, 'contribute.steps_title')}</h2>
    <ol>
      <li>{t(lang, 'contribute.step1')}</li>
      <li>{t(lang, 'contribute.step2')}</li>
      <li>{t(lang, 'contribute.step3')}</li>
      <li>{t(lang, 'contribute.step4')}</li>
    </ol>
  </div>
</BaseLayout>
```

**Commit:** `feat: add contribute page`

---

## Step 32 — Favicon

**File:** `public/favicon.svg`

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 36 36">
  <text x="0" y="28" font-size="28">✉️</text>
</svg>
```

**Commit:** `chore: add favicon`

---

## Step 33 — GitHub Actions CI workflow

**File:** `.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate-and-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate YAML schemas
        run: npm run validate

      - name: Run tests
        run: npm test

      - name: Build site
        run: npm run build
```

**Commit:** `ci: add GitHub Actions workflow for validation, tests, and build`

---

## Step 34 — GitHub Issue template for price reports

**File:** `.github/ISSUE_TEMPLATE/price-correction.md`

```markdown
---
name: Price Correction
about: Report an incorrect postal rate
title: "[Price] OPERATOR_NAME - PRODUCT_NAME"
labels: price-correction
---

## Operator
<!-- Name of the postal operator (e.g., La Poste, Deutsche Post) -->


## Product
<!-- Name of the product and current incorrect price -->
- Product name:
- Current price in PostCompare:
- Correct price:

## Route
<!-- Domestic or international? -->
- Type: letter / parcel
- Route: domestic / COUNTRY → COUNTRY

## Source
<!-- Link to the official source showing the correct price -->

## Date verified
<!-- When did you check this price? -->
```

**Commit:** `chore: add GitHub Issue template for price corrections`

---

## Step 35 — Remaining operator data files (28 countries)

This step creates YAML files for the remaining 28 operators. Each file follows the exact same schema as the first 4. The data should be collected from each operator's official website.

**Files to create** (one per operator, following the same pattern as steps 7-10):

- `data/operators/poste-italiane-it.yaml` — Poste Italiane (Italy)
- `data/operators/postnl-nl.yaml` — PostNL (Netherlands)
- `data/operators/bpost-be.yaml` — bpost (Belgium)
- `data/operators/ctt-pt.yaml` — CTT (Portugal)
- `data/operators/oesterreichische-post-at.yaml` — Österreichische Post (Austria)
- `data/operators/la-poste-suisse-ch.yaml` — La Poste Suisse (Switzerland)
- `data/operators/postnord-se.yaml` — PostNord SE (Sweden)
- `data/operators/postnord-dk.yaml` — PostNord DK (Denmark)
- `data/operators/posten-norge-no.yaml` — Posten Norge (Norway)
- `data/operators/posti-fi.yaml` — Posti (Finland)
- `data/operators/an-post-ie.yaml` — An Post (Ireland)
- `data/operators/poczta-polska-pl.yaml` — Poczta Polska (Poland)
- `data/operators/ceska-posta-cz.yaml` — Ceska posta (Czech Republic)
- `data/operators/posta-romana-ro.yaml` — Posta Romana (Romania)
- `data/operators/magyar-posta-hu.yaml` — Magyar Posta (Hungary)
- `data/operators/elta-gr.yaml` — ELTA (Greece)
- `data/operators/bgpost-bg.yaml` — Bulgarian Posts (Bulgaria)
- `data/operators/hrvatska-posta-hr.yaml` — Hrvatska posta (Croatia)
- `data/operators/slovenska-posta-sk.yaml` — Slovenska posta (Slovakia)
- `data/operators/posta-slovenije-si.yaml` — Posta Slovenije (Slovenia)
- `data/operators/lietuvos-pastas-lt.yaml` — Lietuvos pastas (Lithuania)
- `data/operators/latvijas-pasts-lv.yaml` — Latvijas Pasts (Latvia)
- `data/operators/omniva-ee.yaml` — Omniva (Estonia)
- `data/operators/cyprus-post-cy.yaml` — Cyprus Post (Cyprus)
- `data/operators/post-luxembourg-lu.yaml` — POST Luxembourg (Luxembourg)
- `data/operators/maltapost-mt.yaml` — MaltaPost (Malta)
- `data/operators/islandspostur-is.yaml` — Islandspostur (Iceland)
- `data/operators/post-li.yaml` — Liechtenstein Post (Liechtenstein)

For each file, the implementor must:
1. Visit the official website listed in the design spec
2. Collect standard letter rates (domestic + international zones to Europe)
3. Collect standard parcel rates (domestic + international zones to Europe)
4. Convert non-EUR prices using `data/exchange-rates.yaml` rates
5. Run `npm run validate` after each file to confirm schema compliance
6. Commit each operator individually or in small batches (3-5 at a time)

**Note:** This is a research-heavy task. It can be parallelized across multiple agents, with each agent handling a batch of countries. Prioritize larger/more commonly shipped-to countries first: IT, NL, BE, PT, AT, CH, SE, PL, then the rest.

**Commit pattern:** `feat: add OPERATOR_NAME (COUNTRY) operator data`

---

## Step 36 — SVG placeholder logos

**File:** `public/logos/placeholder.svg`

Create a simple generic postal stamp SVG placeholder to use until real operator logos are obtained. Each operator's `logo` field in YAML points to a file in `public/logos/`. For V1, all operators can reference the same placeholder or use emoji flags in the UI (already done in the components). Real logos are a future enhancement.

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 48 48">
  <rect width="48" height="48" rx="6" fill="#fdf6e3" stroke="#c4a35a" stroke-width="2" stroke-dasharray="4 2"/>
  <text x="24" y="30" text-anchor="middle" font-size="20">✉</text>
</svg>
```

**Commit:** `chore: add placeholder SVG logo for operators`

---

## Step 37 — README

**File:** `README.md`

```markdown
# PostCompare.eu

Free and independent European postal rate comparator. Compare letter and parcel prices across 32 European national postal services.

## Features

- Compare domestic postal rates across all European countries
- Compare international shipping rates between European countries
- Filter by shipment type (letter/parcel) and weight
- Available in 3 languages (FR, EN, DE)
- Open source data maintained by the community

## Tech Stack

- [Astro](https://astro.build) — Static site generator
- [React](https://react.dev) — Interactive components
- TypeScript
- YAML data files validated by JSON Schema

## Getting Started

\`\`\`bash
npm install
npm run dev
\`\`\`

## Commands

| Command | Description |
|---------|-------------|
| \`npm run dev\` | Start dev server |
| \`npm run build\` | Build static site |
| \`npm run preview\` | Preview built site |
| \`npm test\` | Run tests |
| \`npm run validate\` | Validate YAML data against schema |

## Contributing

See the [Contribute page](https://postcompare.eu/en/contribute) or open a GitHub Issue.

### Reporting incorrect prices

Open a GitHub Issue with the operator name, product, and correct price with a link to the official source.

### Fixing prices directly

1. Fork this repo
2. Edit the YAML file in \`data/operators/\`
3. Run \`npm run validate\` to check your changes
4. Open a Pull Request

## License

MIT
\`\`\`

**Commit:** `docs: add README`

---

## Step 38 — Final verification

Run the complete verification checklist from the design spec:

\`\`\`bash
# 1. Start dev server and verify the site loads
npm run dev

# 2. Test comparator: letter 20g FR→DE (in browser)
# Verify results display sorted by price

# 3. Test domestic parcel: 1kg FR→FR (in browser)
# Verify domestic rates appear

# 4. Test responsive: resize browser to mobile width
# Verify form collapses to summary pills

# 5. Test operator page: click "View rates" on a result
# Verify all rate tables display

# 6. Validate YAML schemas
npm run validate

# 7. Run tests
npm test

# 8. Build the static site
npm run build
\`\`\`

**Commit:** (no commit — verification only)

---

## Dependency Graph

```
Step 1 (scaffold)
├── Step 2 (directories)
├── Step 3 (types) ─── independent
├── Step 4 (schema) ── independent
│
├── Step 5 (countries.yaml) ── independent
├── Step 6 (exchange-rates.yaml) ── independent
│
├── Steps 7-10 (operator data) ── depend on Step 4 for validation
│
├── Step 11 (data-loader) ── depends on Step 3
├── Step 14 (vitest config) ── depends on Step 1
│
├── Step 12 (tests) ── depends on Steps 3, 14
├── Step 13 (comparison engine) ── depends on Steps 3, 12
├── Step 15 (validation script) ── depends on Steps 4, 7-10
│
├── Steps 16-17 (i18n) ── depends on Step 3
│
├── Step 18 (CSS) ── independent
├── Step 19 (BaseLayout) ── depends on Steps 18, 20, 22
├── Steps 20-22 (Header, LanguagePicker, Footer) ── depend on Step 17
│
├── Step 23 (ComparatorForm) ── depends on Steps 3, 17
├── Step 24 (ResultCard) ── depends on Steps 3, 17
├── Step 25 (ResultsList) ── depends on Step 24
├── Step 26 (Comparator) ── depends on Steps 13, 23, 25
│
├── Step 27 (index redirect) ── independent
├── Step 28 (home page) ── depends on Steps 11, 19, 26
├── Step 29 (operator page) ── depends on Steps 11, 19, 17
├── Steps 30-31 (about, contribute) ── depend on Steps 19, 17
│
├── Step 32 (favicon) ── independent
├── Step 33 (CI) ── depends on Steps 14, 15
├── Step 34 (issue template) ── independent
│
├── Step 35 (remaining operators) ── depends on Steps 4, 6
├── Step 36 (logos) ── independent
├── Step 37 (README) ── independent
│
└── Step 38 (verification) ── depends on all above
```

## Parallelization Groups

The following steps can be worked on simultaneously by separate agents:

**Group A (Data Foundation):** Steps 3, 4, 5, 6 — all independent
**Group B (Operator Data):** Steps 7, 8, 9, 10 — all independent, need Step 4
**Group C (Engine):** Steps 11, 12, 13, 14, 15 — sequential chain
**Group D (i18n + Static Content):** Steps 16, 17, 30, 31, 34, 37 — mostly independent
**Group E (UI Components):** Steps 18, 19, 20, 21, 22, 23, 24, 25, 26 — sequential chain
**Group F (Pages):** Steps 27, 28, 29 — depend on Groups C + E
**Group G (Remaining Data):** Step 35 — can be heavily parallelized (7 agents, 4 countries each)
